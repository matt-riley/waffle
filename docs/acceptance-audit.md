# Acceptance-Criteria Audit

Source snapshot: `docs/acceptance-audit/issues.json`

| ID | Criterion | Status | Implementation/artifact | Automated evidence | Manual/gated evidence | Current verification |
|---|---|---|---|---|---|---|
| #8.1 | Make the injected summary a `RoleUser` message (the labeling text already defends against prompt injection), **or** carry it as extra `System` text on the `Request` instead of as a message. | pass | `internal/agent/agent.go:prepareContext`; `internal/llm/anthropicp/anthropic.go:toParams` | `TestRunSummarizeAndTruncate`; `TestSummaryInSystemNotAsMessage` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #8.2 | If kept as a message, also guarantee alternation at the window boundary (e.g. ensure the first `recent` message is a user turn, or merge the summary into the first user message). | not-applicable | `internal/agent/agent.go:prepareContext`; `internal/llm/anthropicp/anthropic.go:toParams` | conditional criterion; summary is not kept as a message | chosen system-text design makes message-boundary alternation inapplicable | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #8.3 | Add a provider-translation test asserting the first outgoing Anthropic message is always `user` role. | pass | `internal/agent/agent.go:prepareContext`; `internal/llm/anthropicp/anthropic.go:toParams` | `TestRunSummarizeAndTruncate`; `TestSummaryInSystemNotAsMessage` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #9.1 | Derive a detached drain context for accepted work, e.g. `drainCtx := context.WithoutCancel(ctx)` (optionally with a bounded `context.WithTimeout`), and pass it into `handle`/`Agent.Run`/`AppendTurn` for the in-flight turn on the shutdown path — while still using the cancelable `ctx` to stop *accepting* new messages. | pass | `internal/gateway/gateway.go:Run` (`context.WithoutCancel`) | `TestGracefulShutdownPersistsTurn` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #9.2 | Add a test that cancels mid-turn and asserts the turn is persisted. | pass | `internal/gateway/gateway.go:Run` (`context.WithoutCancel`) | `TestGracefulShutdownPersistsTurn` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #10.1 | Await the scheduler before running cleanup. | pass | `cmd/waffle/serve_cmd.go:serveCmd`; `internal/schedule/schedule.go:Scheduler.Run` | scheduler shutdown ordering inspected; `TestSchedulerReconcilesJobChanges` covers drained scheduler runtime | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #10.2 | Ensure `Scheduler.Run`'s in-flight-job drain (`c.Stop()`) completes before shared resources are torn down. | pass | `cmd/waffle/serve_cmd.go:serveCmd`; `internal/schedule/schedule.go:Scheduler.Run` | scheduler shutdown ordering inspected; `TestSchedulerReconcilesJobChanges` covers drained scheduler runtime | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #10.3 | Add a test that SIGTERMs mid-cron-job and asserts no use-after-close. | pass | `cmd/waffle/serve_cmd.go:serveCmd`; `internal/schedule/schedule.go:Scheduler.Run` | `TestAcceptanceIssue10SchedulerCancellationDrainsInFlightJob`; `TestAcceptanceIssue10ShutdownWaitsForInFlightCronBeforeCleanup` (RED/GREEN seams recorded in report) | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #11.1 | Refuse on anything other than a definitive not-found. | pass | `internal/secret/identity.go:LoadIdentity`, `InitIdentity` | `TestInitIdentityRefusesWhenIdentityExists`; `TestInitIdentityRefusesOnTransientKeyringError`; `TestLoadIdentitySeparatesNotFoundFromUnavailable` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #11.2 | `errors.Is(err, ErrNotFound)` (or `ErrNoIdentity`) → safe to create. | pass | `internal/secret/identity.go:LoadIdentity`, `InitIdentity` | `TestInitIdentityRefusesWhenIdentityExists`; `TestInitIdentityRefusesOnTransientKeyringError`; `TestLoadIdentitySeparatesNotFoundFromUnavailable` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #11.3 | `nil` → identity exists → refuse. | pass | `internal/secret/identity.go:LoadIdentity`, `InitIdentity` | `TestInitIdentityRefusesWhenIdentityExists`; `TestInitIdentityRefusesOnTransientKeyringError`; `TestLoadIdentitySeparatesNotFoundFromUnavailable` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #11.4 | any other error → **refuse** ("keyring unavailable; cannot verify existing identity — refusing to overwrite"), do not create. | pass | `internal/secret/identity.go:LoadIdentity`, `InitIdentity` | `TestInitIdentityRefusesWhenIdentityExists`; `TestInitIdentityRefusesOnTransientKeyringError`; `TestLoadIdentitySeparatesNotFoundFromUnavailable` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #11.5 | Add a test injecting a transient keyring error and asserting no overwrite occurs. | pass | `internal/secret/identity.go:LoadIdentity`, `InitIdentity` | `TestInitIdentityRefusesWhenIdentityExists`; `TestInitIdentityRefusesOnTransientKeyringError`; `TestLoadIdentitySeparatesNotFoundFromUnavailable` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #12.1 | Add `StreamOptions *struct{ IncludeUsage bool }` (json `stream_options`) to `wireRequest` and set `include_usage: true` when `Stream` is true. | pass | `internal/llm/openaip/openai.go:wireRequest`, `Complete` | `TestCompleteText`; `TestUsageOnlyFinalChunk` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #12.2 | Handle the final usage-only chunk (which has an empty `choices` array). | pass | `internal/llm/openaip/openai.go:wireRequest`, `Complete` | `TestCompleteText`; `TestUsageOnlyFinalChunk` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #12.3 | Add a test asserting the request body includes `stream_options.include_usage` and that a usage-bearing final chunk populates `resp.Usage`. | pass | `internal/llm/openaip/openai.go:wireRequest`, `Complete` | `TestCompleteText`; `TestUsageOnlyFinalChunk` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #13.1 | Do not let text spend budget that then starves tool calls. | pass | `internal/llm/openaip/openai.go:Complete` separate text/tool budgets | `TestTextCapDoesNotDropToolCall`; `TestCompleteToolCallKeptWhenAnotherTruncated` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #13.2 | Options: track separate budgets for text vs tool-call arguments; or when the cap is hit, **preserve** already-complete tool calls and keep `StopToolUse` (truncate only the streamed text). | pass | `internal/llm/openaip/openai.go:Complete` separate text/tool budgets | `TestTextCapDoesNotDropToolCall`; `TestCompleteToolCallKeptWhenAnotherTruncated` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #13.3 | Add a test where text nearly fills the budget and a subsequent tool call must still be returned. | pass | `internal/llm/openaip/openai.go:Complete` separate text/tool budgets | `TestTextCapDoesNotDropToolCall`; `TestCompleteToolCallKeptWhenAnotherTruncated` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #14.1 | Measure the no-heartbeat window from runner readiness, not Exec entry. | pass | `internal/sandbox/client.go:Client.Exec` startup grace | `TestExecToleratesColdStartFirstHeartbeat` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #14.2 | Either (a) have `StartDocker` block until the first heartbeat row appears (bounded readiness handshake) before returning, or (b) record the container start time and only apply `runnerNoHealthWait` relative to it, or (c) treat "no heartbeat yet, container young" as "still starting" and keep polling up to a separate, longer startup budget. | pass | `internal/sandbox/client.go:Client.Exec` startup grace | `TestExecToleratesColdStartFirstHeartbeat` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #14.3 | Add a test simulating a slow first heartbeat. | pass | `internal/sandbox/client.go:Client.Exec` startup grace | `TestExecToleratesColdStartFirstHeartbeat` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #15.1 | Distinguish "health probe couldn't run" from "runner is alive". | pass | `internal/sandbox/client.go:Client.Exec`, `lastHealth` | `TestExecPersistentProbeFailuresFailFast`; `TestExecDetectsDeadRunnerAfterTransientLockContention` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #15.2 | On `lastHealth` deadline/contention, don't silently treat it as healthy — e.g. track consecutive probe failures and, past a threshold within the no-heartbeat window, run `attemptShutdown` and fail fast; or raise the `lastHealth` timeout above the queue `busy_timeout` so a genuine result is obtained; or fall back to a container-liveness check (`docker inspect`) when the SQLite probe times out. | pass | `internal/sandbox/client.go:Client.Exec`, `lastHealth` | `TestExecPersistentProbeFailuresFailFast`; `TestExecDetectsDeadRunnerAfterTransientLockContention` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #15.3 | Add a test with a contended/oversized busy window and a dead runner. | pass | `internal/sandbox/client.go:Client.Exec`, `lastHealth` | `TestExecPersistentProbeFailuresFailFast`; `TestExecDetectsDeadRunnerAfterTransientLockContention` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #16.1 | Have `Open` branch on the error kind: only proceed to create when the lookup is a definitive not-found (a sentinel `ErrWorkspaceNotFound` from `scanOne` via `errors.Is`); on any other error, return it (or retry) rather than creating a duplicate. | pass | `internal/workspace/workspace.go:Manager.Open`, `ErrWorkspaceNotFound` | `TestForRepoReturnsNotFoundSentinel`; `TestOpenLookupErrorDoesNotCreateWorkspace` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #16.2 | Add a test injecting a transient error from `ForRepo` and asserting no new container/volume is created. | pass | `internal/workspace/workspace.go:Manager.Open`, `ErrWorkspaceNotFound` | `TestForRepoReturnsNotFoundSentinel`; `TestOpenLookupErrorDoesNotCreateWorkspace` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #17.1 | Reconcile the cron set with the store periodically or on demand. | pass | `internal/schedule/schedule.go:Scheduler.reconcile` registered entry map | `TestSchedulerReconcilesJobChanges` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #17.2 | Options: a ticker that re-lists jobs and diffs against the registered entries (add new, remove deleted, update changed cron specs); or have `cron add`/enable signal the running scheduler (e.g. via a channel or a lightweight notify) to reload. | pass | `internal/schedule/schedule.go:Scheduler.reconcile` registered entry map | `TestSchedulerReconcilesJobChanges` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #17.3 | Track registered `cron.EntryID`s per job id so updates/removals are precise. | pass | `internal/schedule/schedule.go:Scheduler.reconcile` registered entry map | `TestSchedulerReconcilesJobChanges` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #17.4 | Add a test that adds a job after `Run` has started and asserts it fires. | pass | `internal/schedule/schedule.go:Scheduler.reconcile` registered entry map | `TestSchedulerReconcilesJobChanges` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #18.1 | Only switch `c.current` after `Turns` succeeds — move `c.current = sess` inside the success branch, and on error surface it and leave the session unchanged (or fully reset history/persisted/current together atomically). | pass | `cmd/waffle/chat_cmd.go:switchToWorkspaceSession` | `TestSwitchToWorkspaceSessionKeepsStateOnTurnsError`; `TestSwitchToWorkspaceSessionLoadsHistory` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #18.2 | Add a test where `Turns` errors and assert `c.current` is unchanged. | pass | `cmd/waffle/chat_cmd.go:switchToWorkspaceSession` | `TestSwitchToWorkspaceSessionKeepsStateOnTurnsError`; `TestSwitchToWorkspaceSessionLoadsHistory` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #19.1 | `waffle ws close <a> <b>` is rejected with a usage error that names **both** conflicting tokens, before any store lookup or Docker call is made. | pass | `cmd/waffle/ws_cmd.go:parseWorkspaceCloseArgs` (parse precedes manager construction) | `TestParseWorkspaceCloseArgs` table covers both positions, unknown/extra/missing args | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #19.2 | Any flag-shaped argument other than `--force` (leading `-`) is rejected with a usage error naming the unknown flag — it is never treated as a workspace id. | pass | `cmd/waffle/ws_cmd.go:parseWorkspaceCloseArgs` (parse precedes manager construction) | `TestParseWorkspaceCloseArgs` table covers both positions, unknown/extra/missing args | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #19.3 | `--force` is accepted in either position: `ws close <id> --force` and `ws close --force <id>` both work and both set force. | pass | `cmd/waffle/ws_cmd.go:parseWorkspaceCloseArgs` (parse precedes manager construction) | `TestParseWorkspaceCloseArgs` table covers both positions, unknown/extra/missing args | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #19.4 | `ws close` with no id still returns the usage error (existing behaviour preserved). | pass | `cmd/waffle/ws_cmd.go:parseWorkspaceCloseArgs` (parse precedes manager construction) | `TestParseWorkspaceCloseArgs` table covers both positions, unknown/extra/missing args | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #19.5 | On any parse rejection the process exits non-zero and no workspace mutation occurs — verified by test (no `Manager.Close`/`Idle` invocation; the parse must fail before the manager is constructed or via a stub). | pass | `cmd/waffle/ws_cmd.go:parseWorkspaceCloseArgs` (parse precedes manager construction) | `TestWorkspaceCloseParseFailureStopsBeforeStoreMutation`; `TestParseWorkspaceCloseArgs` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #19.6 | Unit tests (table-driven) cover at minimum: `close a b` → error naming `a` and `b`; `close a --froce` → error naming `--froce`; `close a --force` → ok/force; `close --force a` → ok/force; `close` → usage; `close --force` → usage. | pass | `cmd/waffle/ws_cmd.go:parseWorkspaceCloseArgs` (parse precedes manager construction) | `TestParseWorkspaceCloseArgs` table covers both positions, unknown/extra/missing args | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #19.7 | No behaviour change to `ls`/`open`/`idle`. | pass | `cmd/waffle/ws_cmd.go:parseWorkspaceCloseArgs` (parse precedes manager construction) | `TestParseWorkspaceCloseArgs` table covers both positions, unknown/extra/missing args | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #20.1 | `waffle cron add <name> <5 cron fields> <prompt> --deliver` (trailing, no value) exits non-zero with a usage error that names the flag and the expected value shape; **no job row is created** (assert via `jobs.List`). | pass | `cmd/waffle/cron_cmd.go:splitCronFlags`, `splitDeliver`; `schedule.ParseTarget` | `TestSplitDeliver`; `TestCronAddRejectsMissingDeliverValueWithoutCreatingJob`; `TestCronAddPersistsDeliveryOutsidePrompt` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #20.2 | `--deliver telegram:900` in any position (before or after the prompt words) still parses: prompt excludes the flag pair, `Deliver == "telegram:900"`. | pass | `cmd/waffle/cron_cmd.go:splitCronFlags`, `splitDeliver`; `schedule.ParseTarget` | `TestSplitDeliver`; `TestCronAddRejectsMissingDeliverValueWithoutCreatingJob`; `TestCronAddPersistsDeliveryOutsidePrompt` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #20.3 | A `--deliver` value that `schedule.ParseTarget` rejects (e.g. `--deliver banana`) is a add-time usage error, not a stored job. | pass | `cmd/waffle/cron_cmd.go:splitCronFlags`, `splitDeliver`; `schedule.ParseTarget` | `TestSplitDeliver`; `TestCronAddRejectsMissingDeliverValueWithoutCreatingJob`; `TestCronAddPersistsDeliveryOutsidePrompt` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #20.4 | The stored prompt never contains the literal token `--deliver` in any accepted invocation (test asserts on the persisted `Job.Prompt`). | pass | `cmd/waffle/cron_cmd.go:splitCronFlags`, `splitDeliver`; `schedule.ParseTarget` | `TestSplitDeliver`; `TestCronAddRejectsMissingDeliverValueWithoutCreatingJob`; `TestCronAddPersistsDeliveryOutsidePrompt` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #20.5 | Unit tests (table-driven) for `splitDeliver` cover: no flag; flag mid-args; flag last-with-value; flag trailing-without-value; value that fails `ParseTarget`; flag given twice. | pass | `cmd/waffle/cron_cmd.go:splitCronFlags`, `splitDeliver`; `schedule.ParseTarget` | `TestSplitDeliver`; `TestCronAddRejectsMissingDeliverValueWithoutCreatingJob`; `TestCronAddPersistsDeliveryOutsidePrompt` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #20.6 | `cron add` usage text (`cronUsage`, `cron_cmd.go:100-113`) documents the `channel:chat_id` shape (it already shows an example; keep it in sync with the new error message). | pass | `cmd/waffle/cron_cmd.go:splitCronFlags`, `splitDeliver`; `schedule.ParseTarget` | `TestSplitDeliver`; `TestCronAddRejectsMissingDeliverValueWithoutCreatingJob`; `TestCronAddPersistsDeliveryOutsidePrompt` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #21.1 | Insert an explicit end-of-options separator: `git checkout -- <ref>` (or use `git switch --detach <ref>` with validation), and reject refs starting with `-`. | pass | `internal/selfdev/selfdev.go:Upgrade`, `validateRef` | `TestUpgradeRejectsOptionRefs`; `TestValidateRef` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #21.2 | Apply the same pattern to any other place a ref/branch flows into a git argument. | pass | `internal/selfdev/selfdev.go:Upgrade`, `validateRef` | `TestUpgradeRejectsOptionRefs`; `TestValidateRef` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #22.1 | Default new `write_file` files to `0o600`. | pass | `internal/tool/builtin.go` write-file creation mode `0600` | `TestWriteFilePermissions`; `TestFileTools` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #22.2 | If a broader default is wanted for non-secret output, honor the process umask or an explicit mode arg — but the safe default should be owner-only, matching the rest of the tree. | pass | `internal/tool/builtin.go` write-file creation mode `0600` | `TestWriteFilePermissions`; `TestFileTools` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #23.1 | `fetch` of a URL whose host resolves to loopback, link-local (including `169.254.169.254`), unspecified, or RFC1918/ULA ranges fails with the documented error, by default, in both host and sandbox toolboxes. | pass | `internal/tool/builtin.go:Fetch.transport`, `parseFetchPolicy`, `blockedFetchAddr`; `internal/config/config.go:FetchConfig` | `TestFetch`; `TestFetchBlocksPrivateAndAllowsHostPort`; `TestFetchAddressClasses`; `TestFetchRedirectToPrivateIsRefused`; config allowlist validation tests | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #23.2 | The check happens at **dial time** on the connected address: a test with a custom resolver (or a hosts-file-style stub) that returns a public IP at first resolution and `127.0.0.1` at dial time still gets refused (DNS-rebind regression test). | pass | `internal/tool/builtin.go:Fetch.transport`, `parseFetchPolicy`, `blockedFetchAddr`; `internal/config/config.go:FetchConfig` | `TestFetchRejectsDNSRebindAtDialTime`; `TestFetchRedirectToPrivateIsRefused`; `TestFetchAddressClasses` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #23.3 | A redirect from an allowed public URL to a private address is refused (test with `httptest` server issuing a 302 to `http://127.0.0.1:<port>`). | pass | `internal/tool/builtin.go:Fetch.transport`, `parseFetchPolicy`, `blockedFetchAddr`; `internal/config/config.go:FetchConfig` | `TestFetch`; `TestFetchBlocksPrivateAndAllowsHostPort`; `TestFetchAddressClasses`; `TestFetchRedirectToPrivateIsRefused`; config allowlist validation tests | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #23.4 | `[tools.fetch] allow_private` entries (CIDR and host:port forms) permit exactly the listed destinations; an empty/absent config permits none (tests for both). | pass | `internal/tool/builtin.go:Fetch.transport`, `parseFetchPolicy`, `blockedFetchAddr`; `internal/config/config.go:FetchConfig` | `TestFetch`; `TestFetchBlocksPrivateAndAllowsHostPort`; `TestFetchAddressClasses`; `TestFetchRedirectToPrivateIsRefused`; config allowlist validation tests | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #23.5 | IPv6 equivalents are covered (`::1`, `fe80::/10`, `fc00::/7`) — at minimum unit tests over the range predicate. | pass | `internal/tool/builtin.go:Fetch.transport`, `parseFetchPolicy`, `blockedFetchAddr`; `internal/config/config.go:FetchConfig` | `TestFetch`; `TestFetchBlocksPrivateAndAllowsHostPort`; `TestFetchAddressClasses`; `TestFetchRedirectToPrivateIsRefused`; config allowlist validation tests | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #23.6 | The refusal error names the resolved IP and the config key; no partial response body is ever returned on refusal. | pass | `internal/tool/builtin.go:Fetch.transport`, `parseFetchPolicy`, `blockedFetchAddr`; `internal/config/config.go:FetchConfig` | `TestFetchRejectsDNSRebindAtDialTime`; `TestFetchRedirectToPrivateIsRefused`; `TestFetchAddressClasses` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #23.7 | Existing `TestFetch` (`internal/tool/tool_test.go:106`) keeps passing — note it uses an `httptest` loopback server, so the test toolbox must opt in via the allowlist mechanism (this doubles as the allowlist test). | pass | `internal/tool/builtin.go:Fetch.transport`, `parseFetchPolicy`, `blockedFetchAddr`; `internal/config/config.go:FetchConfig` | `TestFetch`; `TestFetchBlocksPrivateAndAllowsHostPort`; `TestFetchAddressClasses`; `TestFetchRedirectToPrivateIsRefused`; config allowlist validation tests | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #23.8 | README/docs note the new default and the escape hatch. | pass | `internal/tool/builtin.go:Fetch.transport`, `parseFetchPolicy`, `blockedFetchAddr`; `internal/config/config.go:FetchConfig` | `TestFetch`; `TestFetchBlocksPrivateAndAllowsHostPort`; `TestFetchAddressClasses`; `TestFetchRedirectToPrivateIsRefused`; config allowlist validation tests | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #24.1 | `migrate` applies every embedded migration whose version is not present in `schema_migrations`, including versions lower than the current max; each application inserts its row. | pass | `internal/store/store.go:loadMigrations`, `pending`, `migrate`, `applyOne` | `TestLoadMigrationsSortedAndUnique`; `TestValidateContiguous`; `TestPendingSkipsApplied`; `TestMigrateAppliesOutOfOrder`; `TestOpenIsIdempotent` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #24.2 | Test: apply `{1,2,4}` (simulate branch A), then add `3` to the embedded set and re-run `migrate` → `3` is applied, recorded, and its schema objects exist (query them). | pass | `internal/store/store.go:loadMigrations`, `pending`, `migrate`, `applyOne` | `TestLoadMigrationsSortedAndUnique`; `TestValidateContiguous`; `TestPendingSkipsApplied`; `TestMigrateAppliesOutOfOrder`; `TestOpenIsIdempotent` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #24.3 | Test: re-running `migrate` on a fully applied set is a no-op (idempotence preserved). | pass | `internal/store/store.go:loadMigrations`, `pending`, `migrate`, `applyOne` | `TestLoadMigrationsSortedAndUnique`; `TestValidateContiguous`; `TestPendingSkipsApplied`; `TestMigrateAppliesOutOfOrder`; `TestOpenIsIdempotent` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #24.4 | Out-of-order application emits the documented warning (or error, if the strict policy is chosen) naming the migration — asserted in the test. | pass | `internal/store/store.go:loadMigrations`, `pending`, `migrate`, `applyOne` | `TestLoadMigrationsSortedAndUnique`; `TestValidateContiguous`; `TestPendingSkipsApplied`; `TestMigrateAppliesOutOfOrder`; `TestOpenIsIdempotent` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #24.5 | `loadMigrations` rejects an embedded set with a version gap, with an error naming the missing number (test). | pass | `internal/store/store.go:loadMigrations`, `pending`, `migrate`, `applyOne` | `TestLoadMigrationsSortedAndUnique`; `TestValidateContiguous`; `TestPendingSkipsApplied`; `TestMigrateAppliesOutOfOrder`; `TestOpenIsIdempotent` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #24.6 | Duplicate-version rejection still covered by test. | pass | `internal/store/store.go:loadMigrations`, `pending`, `migrate`, `applyOne` | `TestLoadMigrationsSortedAndUnique`; `TestValidateContiguous`; `TestPendingSkipsApplied`; `TestMigrateAppliesOutOfOrder`; `TestOpenIsIdempotent` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #24.7 | The `applyOne` transaction semantics are unchanged: a failing migration applies nothing and records nothing (existing behaviour, add a regression test if none exists). | pass | `internal/store/store.go:loadMigrations`, `pending`, `migrate`, `applyOne` | `TestApplyOneRollsBackFailingMigration` asserts schema object and migration row both roll back | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #24.8 | A comment on `migrate` documents the set-based rule and the out-of-order policy so the next migration author doesn't have to re-derive it. | pass | `internal/store/store.go:loadMigrations`, `pending`, `migrate`, `applyOne` | `TestLoadMigrationsSortedAndUnique`; `TestValidateContiguous`; `TestPendingSkipsApplied`; `TestMigrateAppliesOutOfOrder`; `TestOpenIsIdempotent` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #25.1 | New migration adds `AFTER DELETE` and `AFTER UPDATE` triggers exactly mirroring the external-content FTS5 sync pattern, plus a one-time index `rebuild`. | pass | `internal/store/migrations/0007_turns_fts_sync.sql`; session/store FTS paths | `TestFTSSurvivesSessionDelete`; `TestFTSSurvivesTurnUpdate`; `TestDeleteRemovesSessionTurnsAndFTS` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #25.2 | Test: insert a session with turns, `DELETE FROM sessions WHERE id=…` directly through the store's DB handle, then (a) FTS search for the deleted content returns zero rows, and (b) an FTS5 **integrity-check** (`INSERT INTO turns_fts(turns_fts, rank) VALUES('integrity-check', 1)` in FTS5's external-content mode) passes without error. | pass | `internal/store/migrations/0007_turns_fts_sync.sql`; session/store FTS paths | `TestFTSSurvivesSessionDelete`; `TestFTSSurvivesTurnUpdate`; `TestDeleteRemovesSessionTurnsAndFTS` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #25.3 | Test: `UPDATE turns SET text=…` then search finds the new text and not the old. | pass | `internal/store/migrations/0007_turns_fts_sync.sql`; session/store FTS paths | `TestFTSSurvivesSessionDelete`; `TestFTSSurvivesTurnUpdate`; `TestDeleteRemovesSessionTurnsAndFTS` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #25.4 | Test: search with `snippet(turns_fts, …)` after a cascade delete neither errors nor returns content from deleted turns. | pass | `internal/store/migrations/0007_turns_fts_sync.sql`; session/store FTS paths | `TestFTSSurvivesSessionDelete`; `TestFTSSurvivesTurnUpdate`; `TestDeleteRemovesSessionTurnsAndFTS` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #25.5 | Migration is idempotent under the runner (applies once, recorded once) and passes `doctor`'s migrate-on-snapshot check. | pass | `internal/store/migrations/0007_turns_fts_sync.sql`; session/store FTS paths | `TestFTSSurvivesSessionDelete`; `TestFTSSurvivesTurnUpdate`; `TestDeleteRemovesSessionTurnsAndFTS` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #25.6 | The migration's header comment documents why the triggers exist and supersedes the "insert-only" note in 0002. | pass | `internal/store/migrations/0007_turns_fts_sync.sql`; session/store FTS paths | `TestFTSSurvivesSessionDelete`; `TestFTSSurvivesTurnUpdate`; `TestDeleteRemovesSessionTurnsAndFTS` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #26.1 | Detect the constraint violation structurally rather than by full-message substring: use `modernc.org/sqlite`'s typed error / result code (`SQLITE_CONSTRAINT_UNIQUE`, code 2067) via `errors.As`, falling back to a case-insensitive "unique constraint failed" check without requiring the column/index name. | pass | `internal/workspace/workspace.go:isUniqueConstraintError` typed SQLite code plus fallback | `TestOpenConcurrentRaceResumesWinner`; `TestIsUniqueConstraintErrorRealDriver`; `TestIsUniqueConstraintErrorFallback` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #26.2 | Add a test that forces the partial-index violation and asserts reconciliation resumes the winner. | pass | `internal/workspace/workspace.go:isUniqueConstraintError` typed SQLite code plus fallback | `TestOpenConcurrentRaceResumesWinner`; `TestIsUniqueConstraintErrorRealDriver`; `TestIsUniqueConstraintErrorFallback` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #27.1 | Use `time.Now().UTC().Format("2006-01-02")` for consistency with the rest of the store (or route all timestamping through the shared `now()` helper). | pass | `internal/memory/memory.go:formatNoteLine`, `Workspace.appendCandidate` UTC date | `TestRememberUpdateForget` asserts `time.Now().UTC().Format("2006-01-02")` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #28.1 | Require a word boundary: match the exact command or command-plus-space, e.g. `line == "/repo" \|\| strings.HasPrefix(line, "/repo ")` (same for `/skill`), or tokenize the leading word and switch on it. | pass | `cmd/waffle/chat_cmd.go:splitCommand` exact-token routing | `TestSplitCommand`; `TestSplitCommandDoesNotMisrouteNearMisses`; `TestBareSkillAndRepoStillGiveUsage` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #28.2 | Add tests for `/report`, `/skills`, and the valid forms. | pass | `cmd/waffle/chat_cmd.go:splitCommand` exact-token routing | `TestSplitCommand`; `TestSplitCommandDoesNotMisrouteNearMisses`; `TestBareSkillAndRepoStillGiveUsage` | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #29.1 | A reproduction harness: a Go test (or script) that runs the real queue pair over a Docker Desktop bind mount and hammers both sides (host goroutine inserting requests, containerized runner consuming, for N minutes), asserting: no `SQLITE_BUSY` beyond busy_timeout, every request gets exactly one result, heartbeat gaps never exceed the poll interval ×2. | pass | `internal/sandbox/pids_docker_test.go` real container runner; `internal/selfdev/selfdev.go`; `docs/sandbox-queue.md` | `TestDockerBindMountContainerRunnerStress`; `TestDockerBindMountKillMidWriteIntegrity`; `TestDoctorIncludesSandboxChecksWhenDockerMode` | Docker-gated: `go test -tags=sandbox_docker ./internal/sandbox -run DockerBindMount -count=1 -v`; explicit absent-Docker SKIP here. Manual macOS procedure/expected evidence and primary-source review in `docs/sandbox-queue.md`. | PASS artifact/compile gate; runtime Docker Desktop result gated by absent daemon |
| #29.2 | A crash test: `docker kill` mid-write, then integrity-check both DBs (`PRAGMA integrity_check`). | pass | `internal/sandbox/pids_docker_test.go` real container runner; `internal/selfdev/selfdev.go`; `docs/sandbox-queue.md` | `TestDockerBindMountContainerRunnerStress`; `TestDockerBindMountKillMidWriteIntegrity`; `TestDoctorIncludesSandboxChecksWhenDockerMode` | Docker-gated: `go test -tags=sandbox_docker ./internal/sandbox -run DockerBindMount -count=1 -v`; explicit absent-Docker SKIP here. Manual macOS procedure/expected evidence and primary-source review in `docs/sandbox-queue.md`. | PASS artifact/compile gate; runtime Docker Desktop result gated by absent daemon |
| #29.3 | A documentation check: current Docker Desktop/VirtioFS statements on POSIX lock and fsync passthrough (VirtioFS has materially improved since gRPC-FUSE; the risk may be historical). Record findings either way. | pass | `internal/sandbox/pids_docker_test.go` real container runner; `internal/selfdev/selfdev.go`; `docs/sandbox-queue.md` | `TestDockerBindMountContainerRunnerStress`; `TestDockerBindMountKillMidWriteIntegrity`; `TestDoctorIncludesSandboxChecksWhenDockerMode` | Docker-gated: `go test -tags=sandbox_docker ./internal/sandbox -run DockerBindMount -count=1 -v`; explicit absent-Docker SKIP here. Manual macOS procedure/expected evidence and primary-source review in `docs/sandbox-queue.md`. | PASS artifact/compile gate; runtime Docker Desktop result gated by absent daemon |
| #29.4 | The reproduction harness exists in-tree (build-tagged or opt-in via env var so CI on Linux isn't blocked by it) and its README section documents how to run it on a macOS host. | pass | `internal/sandbox/pids_docker_test.go` real container runner; `internal/selfdev/selfdev.go`; `docs/sandbox-queue.md` | `TestDockerBindMountContainerRunnerStress`; `TestDockerBindMountKillMidWriteIntegrity`; `TestDoctorIncludesSandboxChecksWhenDockerMode` | Docker-gated: `go test -tags=sandbox_docker ./internal/sandbox -run DockerBindMount -count=1 -v`; explicit absent-Docker SKIP here. Manual macOS procedure/expected evidence and primary-source review in `docs/sandbox-queue.md`. | PASS artifact/compile gate; runtime Docker Desktop result gated by absent daemon |
| #29.5 | Findings are recorded on this issue: pass/fail per failure mode, Docker Desktop + VirtioFS version tested. | pass | GitHub owner findings/support comments | n/a | Exact evidence: https://github.com/matt-riley/waffle/issues/29#issuecomment-4952731710 and https://github.com/matt-riley/waffle/issues/29#issuecomment-4952819113 | PASS: external findings are recorded; later deterministic harness supersedes the earlier host-only test limitation |
| #29.6 | The outcome is one of: (a) documented support statement + close; or (b) a follow-up implementation issue for the chosen transport, with this issue closed as superseded by it. | pass | `internal/sandbox/pids_docker_test.go` real container runner; `internal/selfdev/selfdev.go`; `docs/sandbox-queue.md` | `TestDockerBindMountContainerRunnerStress`; `TestDockerBindMountKillMidWriteIntegrity`; `TestDoctorIncludesSandboxChecksWhenDockerMode` | Docker-gated: `go test -tags=sandbox_docker ./internal/sandbox -run DockerBindMount -count=1 -v`; explicit absent-Docker SKIP here. Manual macOS procedure/expected evidence and primary-source review in `docs/sandbox-queue.md`. | PASS artifact/compile gate; runtime Docker Desktop result gated by absent daemon |
| #29.7 | `waffle doctor` includes a sandbox round-trip check when docker mode is configured, reported like the existing checks (`internal/selfdev/selfdev.go:50-88` `add(...)` pattern). | pass | `internal/sandbox/pids_docker_test.go` real container runner; `internal/selfdev/selfdev.go`; `docs/sandbox-queue.md` | `TestDockerBindMountContainerRunnerStress`; `TestDockerBindMountKillMidWriteIntegrity`; `TestDoctorIncludesSandboxChecksWhenDockerMode` | Docker-gated: `go test -tags=sandbox_docker ./internal/sandbox -run DockerBindMount -count=1 -v`; explicit absent-Docker SKIP here. Manual macOS procedure/expected evidence and primary-source review in `docs/sandbox-queue.md`. | PASS artifact/compile gate; runtime Docker Desktop result gated by absent daemon |
| #32.1 | A session bound to repo A requesting credentials for repo B receives an error (git sees an auth failure), no credential is returned, and a `denied` audit row is written naming the session, requested path, and bound repo. | pass | `internal/broker/broker.go:serveGitCredential`; `cmd/waffle/ws_cmd.go:gitCredentialFromSecrets`, `canonRepoPath` | `TestGitCredentialDenialAuditNamesRequestedAndBoundRepo`; `TestGitCredentialScopesToBoundRepo`; `TestGitCredentialRefusesOtherHosts`; `TestGitCredentialRefusesOnLookupError` (fakes only) | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #32.2 | A session bound to repo A requesting credentials for repo A succeeds (regression: the happy path still works end to end). | pass | `internal/broker/broker.go:serveGitCredential`; `cmd/waffle/ws_cmd.go:gitCredentialFromSecrets`, `canonRepoPath` | `TestGitCredentialDenialAuditNamesRequestedAndBoundRepo`; `TestGitCredentialScopesToBoundRepo`; `TestGitCredentialRefusesOtherHosts`; `TestGitCredentialRefusesOnLookupError` (fakes only) | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #32.3 | A session with **no** workspace binding requesting any repo is denied (deny-by-default), with an audit row. | pass | `internal/broker/broker.go:serveGitCredential`; `cmd/waffle/ws_cmd.go:gitCredentialFromSecrets`, `canonRepoPath` | `TestGitCredentialDenialAuditNamesRequestedAndBoundRepo`; `TestGitCredentialScopesToBoundRepo`; `TestGitCredentialRefusesOtherHosts`; `TestGitCredentialRefusesOnLookupError` (fakes only) | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #32.4 | Path canonicalization is covered by tests: `owner/A`, `/owner/A`, `owner/A.git`, `owner/a` (case) all resolve consistently against a binding for `owner/A`, and none of the variants lets `owner/B` through. | pass | `internal/broker/broker.go:serveGitCredential`; `cmd/waffle/ws_cmd.go:gitCredentialFromSecrets`, `canonRepoPath` | `TestGitCredentialDenialAuditNamesRequestedAndBoundRepo`; `TestGitCredentialScopesToBoundRepo`; `TestGitCredentialRefusesOtherHosts`; `TestGitCredentialRefusesOnLookupError` (fakes only) | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #32.5 | Host mismatch (`host != github.com`) still refused as today (regression). | pass | `internal/broker/broker.go:serveGitCredential`; `cmd/waffle/ws_cmd.go:gitCredentialFromSecrets`, `canonRepoPath` | `TestGitCredentialDenialAuditNamesRequestedAndBoundRepo`; `TestGitCredentialScopesToBoundRepo`; `TestGitCredentialRefusesOtherHosts`; `TestGitCredentialRefusesOnLookupError` (fakes only) | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #32.6 | The audit row schema/fields are consistent with the existing broker audit rows (grant vs. deny distinguishable). | pass | `internal/broker/broker.go:serveGitCredential`; `cmd/waffle/ws_cmd.go:gitCredentialFromSecrets`, `canonRepoPath` | `TestGitCredentialDenialAuditNamesRequestedAndBoundRepo`; `TestGitCredentialScopesToBoundRepo`; `TestGitCredentialRefusesOtherHosts`; `TestGitCredentialRefusesOnLookupError` (fakes only) | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #32.7 | Tests use a fake credential func / in-memory workspace store — no live GitHub calls. | pass | `internal/broker/broker.go:serveGitCredential`; `cmd/waffle/ws_cmd.go:gitCredentialFromSecrets`, `canonRepoPath` | `TestGitCredentialDenialAuditNamesRequestedAndBoundRepo`; `TestGitCredentialScopesToBoundRepo`; `TestGitCredentialRefusesOtherHosts`; `TestGitCredentialRefusesOnLookupError` (fakes only) | n/a | PASS: cohort `go test` command from Task 2 brief (`-count=1`) |
| #33.1 | **Per-session dynamic group routing inside the gateway** — distinct Telegram groups → distinct policies, resolved per session and surviving a store round-trip. `entity.AgentGroup` is still hardcoded to `"main"` for channel sessions. This composes with #34 (group-chat posture), which introduces the non-`main` group concept; best done alongside it. | pass | `internal/entity:GroupFor`; `internal/gateway:agentGroupFor/agentForGroup` | `TestGroupForCreatesGroupChatOnRestrictedTier`; `TestGroupOwnerUsesRestrictedAgentTier` | n/a | PASS: focused RED/GREEN or fresh inspected assertion
| #33.2 | **Persist the resolved policy with the session** so the gateway enforces it per-session across restarts (depends on the routing above). | pass | `internal/entity:GroupFor` persists `channel_groups.agent_group` | `TestGroupForCreatesGroupChatOnRestrictedTier`; `TestGatewayUsesPersistedAgentGroup` | n/a | PASS: focused RED/GREEN or fresh inspected assertion
| #33.3 | **docker executor reliability** for `sandbox = "docker"` on non-linux hosts is handled by #42 (linux runner) — the restricted-host-policy tier is the usable default until then. | pass | internal/config; cmd/waffle agent-group construction | config and agent-group policy tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #33.4 | Config supports `[agent.group.<name>]` with `sandbox` and `tools.allow`/`tools.deny`; parsing tested incl. unknown-group fallback to the documented default. | pass | internal/config; cmd/waffle agent-group construction | config and agent-group policy tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #33.5 | A session created for the `cron` group resolves to that group's policy; a session for `main` resolves to its own — asserted by building both and inspecting the resulting toolbox. | pass | internal/config; cmd/waffle agent-group construction | config and agent-group policy tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #33.6 | By default (no `[agent.group.cron]`), a scheduled job's toolbox **excludes host `bash`** (test). | pass | internal/config; cmd/waffle agent-group construction | config and agent-group policy tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #33.7 | `serve` no longer passes one shared `Agent` to both gateway and scheduler. | pass | internal/config; cmd/waffle agent-group construction | config and agent-group policy tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #33.8 | Existing single-`[sandbox]` configs keep working; the cron behaviour change is documented (README). | pass | internal/config; cmd/waffle agent-group construction | config and agent-group policy tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #33.9 | The gateway resolves per-session policy from the session's agent group, surviving a store round-trip. *(follow-up — see above)* | pass | internal/config; cmd/waffle agent-group construction | config and agent-group policy tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #33.10 | The resolved policy persists with the session. *(follow-up)* | pass | internal/config; cmd/waffle agent-group construction | config and agent-group policy tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #34.1 | `channel.Message` carries a group/chat-type signal, populated by the Telegram adapter from `Chat.Type`; unit test over a group update and a private update. | pass | internal/gateway; internal/channel/telegram | group posture and restricted-tier tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #34.2 | In a group, a message that does **not** mention the bot produces no agent run and no reply (test), for owner and non-owner alike. | pass | internal/gateway; internal/channel/telegram | group posture and restricted-tier tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #34.3 | In a group, a message that **does** mention the bot routes to the agent (test), and the mention token is stripped/handled so it doesn't pollute the prompt. | pass | internal/gateway; internal/channel/telegram | group posture and restricted-tier tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #34.4 | Non-owner messages in a group never produce a pairing-code reply (test); pairing replies still occur in private chats (regression test). | pass | internal/gateway; internal/channel/telegram | group posture and restricted-tier tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #34.5 | Group-originated agent sessions use the restricted policy (no host `bash`), verified against the resulting toolbox — coordinated with #33's mechanism (or a standalone restriction if #33 hasn't landed). | pass | internal/gateway; internal/channel/telegram | group posture and restricted-tier tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #34.6 | Bot username resolution (for mention matching) is fetched/cached, not hardcoded; test uses a stub `getMe`. | pass | internal/gateway; internal/channel/telegram | group posture and restricted-tier tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #34.7 | README documents the group posture (and, until merged, the "don't add to groups" caveat is removed once this lands). | pass | internal/gateway; internal/channel/telegram | group posture and restricted-tier tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #35.1 | `[workspace] egress` config parses with the three values; per-repo override slot exists (even if only default is wired first). Tested. | pass | internal/tool; internal/config | fetch SSRF/redirect/DNS-rebind and egress-policy tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #35.2 | `egress = "none"` produces `--network none` in the generated `docker run` args (assert on `workspaceRunArgs`, `runtime.go` — already unit-testable); `full` produces `bridge`; `allowlist` produces `--network none` + host alias + proxy env. | pass | internal/tool; internal/config | fetch SSRF/redirect/DNS-rebind and egress-policy tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #35.3 | Broker exposes an HTTP egress endpoint that: rejects unauthenticated/unknown `wk_` tokens; denies hosts not on the session policy allowlist (audit `denied` row); for an allowlisted host, injects the configured `Authorization` header and forwards, returning the upstream response, without the container ever receiving the raw header value. | pass | internal/tool; internal/config | fetch SSRF/redirect/DNS-rebind and egress-policy tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #35.4 | An allowlist-mode workspace can reach an allowlisted test host (via `httptest`) through the proxy and **cannot** reach a non-allowlisted host (two tests). | pass | internal/tool; internal/config | fetch SSRF/redirect/DNS-rebind and egress-policy tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #35.5 | The egress proxy refuses to forward to loopback/link-local/private addresses unless explicitly configured (shares #23's range predicate). | pass | internal/tool; internal/config | fetch SSRF/redirect/DNS-rebind and egress-policy tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #35.6 | Per-grant audit rows are written and are distinguishable from git-face and provider-proxy rows. | pass | internal/tool; internal/config | fetch SSRF/redirect/DNS-rebind and egress-policy tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #35.7 | README/docs document the three tiers, the allowlist config, and the "tool calls an API without holding its key" pattern. | pass | internal/tool; internal/config | fetch SSRF/redirect/DNS-rebind and egress-policy tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #36.1 | A `last_active` (or equivalent) timestamp is persisted per workspace and updated on activity; migration is idempotent and passes doctor's migrate check. | pass | internal/workspace | reaper idle/TTL/dirty-workspace tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #36.2 | Under `serve`, a workspace idle longer than `idle_timeout` is stopped and marked idle; a test drives the sweep with a stubbed clock and asserts `Idle` is called for the stale workspace and **not** for a recently-active one. | pass | internal/workspace | reaper idle/TTL/dirty-workspace tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #36.3 | A subsequent request to an auto-idled workspace resumes it (existing `Resume` path) — regression test. | pass | internal/workspace | reaper idle/TTL/dirty-workspace tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #36.4 | Past `close_ttl`, a **clean** workspace is closed via `Manager.Close` (no force); a **dirty** workspace is not closed and instead produces an owner notification — both tested with a stub deliverer and the unpushed-work guard exercised. | pass | internal/workspace | reaper idle/TTL/dirty-workspace tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #36.5 | `[workspace] idle_timeout`/`close_ttl` parse, accept duration strings, and `0` disables the respective timer (tested); defaults documented. | pass | internal/workspace | reaper idle/TTL/dirty-workspace tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #36.6 | The sweep is a no-op when no `serve` is running (CLI-only invocations don't spuriously reap) and is single-owner to avoid the #48 race. | pass | internal/workspace | reaper idle/TTL/dirty-workspace tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #37.1 | `Doctor` performs a provider-reachability check per configured provider using the loaded `cfg`; the `_ = cfg` discard is gone. | pass | internal/selfdev; cmd/waffle doctor | provider and doctor tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #37.2 | With a reachable provider and a valid key, the check reports pass; a test uses a stub/`httptest` provider endpoint (no live API calls). | pass | internal/selfdev; cmd/waffle doctor | provider and doctor tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #37.3 | With no key configured, the check reports `skipped`, not fail (test). | pass | internal/selfdev; cmd/waffle doctor | provider and doctor tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #37.4 | With an unreachable endpoint or rejected auth, the check reports fail, and a test asserts that a failing provider check causes the aggregate `ok` to be false (so the upgrade gate would block the swap). | pass | internal/selfdev; cmd/waffle doctor | provider and doctor tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #37.5 | The check honors a short timeout and does not hang when the endpoint is unreachable (test with a non-routable/blocked address or a stub that never responds + deadline). | pass | internal/selfdev; cmd/waffle doctor | provider and doctor tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #37.6 | Undecryptable/missing key ref is reported as a clear failure/skip (not a panic) — test. | pass | internal/selfdev; cmd/waffle doctor | provider and doctor tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #37.7 | `waffle doctor` output lists the provider check alongside the existing ones. | pass | internal/selfdev; cmd/waffle doctor | provider and doctor tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #38.1 | `search` appears in `Builtins` with schema, description, and result caps documented in the tool description (the model reads it). | pass | internal/tool; internal/session | search, registry, and restricted-toolbox tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #38.2 | Matches return as `path:line: excerpt` with per-file and total caps enforced; a run over a tree with more matches than the cap says so explicitly in the output tail (mirroring `Truncate`'s marker convention). | pass | internal/tool; internal/session | search, registry, and restricted-toolbox tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #38.3 | Binary files are skipped; a test includes a file with NUL bytes and asserts it produces no matches and no error. | pass | internal/tool; internal/session | search, registry, and restricted-toolbox tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #38.4 | `.git` directories are not walked (test). | pass | internal/tool; internal/session | search, registry, and restricted-toolbox tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #38.5 | Invalid regexp returns a tool error naming the pattern, not a panic (test). | pass | internal/tool; internal/session | search, registry, and restricted-toolbox tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #38.6 | Context cancellation mid-walk aborts promptly (test with a short deadline over a large synthetic tree). | pass | internal/tool; internal/session | search, registry, and restricted-toolbox tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #38.7 | The sandbox runner exposes `search` with identical behaviour (covered by the existing sandbox toolbox pass-through tests, extended if needed). | pass | internal/tool; internal/session | search, registry, and restricted-toolbox tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #38.8 | A policy-restricted toolbox of `{read_file, search}` (no bash) can complete a "find where X is defined" exercise in an agent-loop test — the tier is demonstrably usable. | pass | internal/tool; internal/session | search, registry, and restricted-toolbox tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #38.9 | docs/plan.md's tool list matches reality after this lands (either the tool exists, or the plan is amended — checkbox satisfied by whichever resolution is chosen, recorded in #39's sweep). | pass | internal/tool; internal/session | search, registry, and restricted-toolbox tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #39.1 | Each of the four deltas (Gemini, bubbletea/TUI, MCP SDK, channel deps) is annotated in docs/plan.md (or a dedicated "Deviations" section) with: what was built instead, whether the deviation is deliberate/deferred/optional, and the rationale where it's a principle-driven choice. | pass | docs/plan.md; README.md | documentation inspection | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #39.2 | The MCP annotation explicitly states the stdio-only / no-HTTP-SSE capability boundary. | pass | docs/plan.md; README.md | documentation inspection | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #39.3 | Gemini's status is resolved to exactly one of: "deliberate — via OpenAI-compat endpoint" (annotated) or "deferred" (with a linked implementation issue). The plan's repo-layout sketch is corrected to match. | pass | docs/plan.md; README.md | documentation inspection | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #39.4 | The README's "all seven phases implemented" claim is reconciled with the annotations (either qualified, or the deviations are noted as intentional so the claim stays honest). | pass | docs/plan.md; README.md | documentation inspection | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #39.5 | The functional-gap issues are cross-linked from the relevant plan sections: #32, #33, #34, #35, #36, #37, #38, #23 (each is a place where the plan is still the intent and the code isn't there yet). | pass | docs/plan.md; README.md | documentation inspection | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #39.6 | A reader can, after this sweep, tell every plan-vs-code difference apart as deliberate / deferred / optional without reading the source. | pass | docs/plan.md; README.md | documentation inspection | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #40.1 | `[github.app]` config parses; `private_key` resolves through the secret store; absent config falls back to the PAT path (tested). | pass | internal/gitcred; internal/broker | GitHub App mint/cache/scope-denial tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #40.2 | JWT construction is correct: RS256, `iss = app_id`, `iat`/`exp` within GitHub's 10-min bound, verifiable against the public key in a test. | pass | internal/gitcred; internal/broker | GitHub App mint/cache/scope-denial tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #40.3 | The installation-token request sends `repositories` scoped to exactly the requested repo and least-privilege `permissions`; asserted against a **fake GitHub API server** (no live calls in tests). | pass | internal/gitcred; internal/broker | GitHub App mint/cache/scope-denial tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #40.4 | A minted token is cached and reused within its lifetime, and re-minted after (simulated) expiry — test drives both. | pass | internal/gitcred; internal/broker | GitHub App mint/cache/scope-denial tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #40.5 | Scope-mismatch refusal composes with #32: a session bound to repo A requesting B is denied before any mint (shared test with #32, or a test asserting the mint is never attempted for B). | pass | internal/gitcred; internal/broker | GitHub App mint/cache/scope-denial tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #40.6 | Audit rows record the backend and repo scope; distinguishable from PAT-backend rows. | pass | internal/gitcred; internal/broker | GitHub App mint/cache/scope-denial tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #40.7 | Failure to reach GitHub (or a 4xx from the token endpoint) surfaces as a clear broker error, not a hang or a silent empty credential. | pass | internal/gitcred; internal/broker | GitHub App mint/cache/scope-denial tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #40.8 | Docs describe registering the app with `contents: read/write` (+ `pull_requests: write` if self-PRs use it) installed only on waffle's repos. | pass | internal/gitcred; internal/broker | GitHub App mint/cache/scope-denial tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #41.1 | Are there ≥2 real hook use cases today, or is this speculative? (If speculative: adopt tier map, close, revisit.) | pass | docs/plan.md; internal/hooks | documented extension decision and bounded hook-runner tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #41.2 | Who writes hooks — owner only, or shared? (Owner-only → Lua; shared/untrusted → wazero.) | pass | docs/plan.md; internal/hooks | documented extension decision and bounded hook-runner tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #41.3 | Which hook points, exactly? (Each one is API surface to maintain forever; start with two.) | pass | docs/plan.md; internal/hooks | documented extension decision and bounded hook-runner tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #41.4 | Fail-open or fail-closed per hook point? | pass | docs/plan.md; internal/hooks | documented extension decision and bounded hook-runner tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #41.5 | How do hooks interact with per-agent-group policy (#33) — global, or per group? | pass | docs/plan.md; internal/hooks | documented extension decision and bounded hook-runner tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #41.6 | **The tier map is adopted as documented policy** (independent of whether any engine is embedded): tool-shaped extension requests are answered with "write an MCP server", code-shaped ones with "fork + `waffle upgrade`". This is added to docs/plan.md (folds into the #39 sweep) so the answer to "how do I extend waffle?" is on the record. | pass | docs/plan.md; internal/hooks | documented extension decision and bounded hook-runner tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #41.7 | **The decision checklist above is answered explicitly on this issue** — each of the five questions gets a recorded answer (≥2 real use cases or speculative; owner-only or shared; which hook points; fail-open/closed per point; global or per-#33-group). | pass | docs/plan.md; internal/hooks | documented extension decision and bounded hook-runner tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #41.8 | **Terminal state A (defer):** if hook use cases are speculative (<2 concrete needs), this issue is closed as "tier map adopted; embedded runtime deferred", with a note on what would reopen it. No engine dependency is added. | pass | docs/plan.md; internal/hooks | documented extension decision and bounded hook-runner tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #41.9 | **Terminal state B (build):** if ≥2 concrete owner-authored hook use cases exist, a follow-up **implementation** issue is filed for a `hooks.Runner` interface with exactly two initial hook points (inbound message, tool result), embedding **gopher-lua**, and it carries these security-rule acceptance criteria: | not-applicable | Owner decision comment 4953083450 selects Terminal A | Terminal B-only criterion | https://github.com/matt-riley/waffle/issues/41#issuecomment-4953083450 | N/A: Terminal A selected
| #41.10 | Hooks receive values only — no filesystem, network, or env ambient authority; any credential/fetch need routes through the broker (composes with #35). | not-applicable | Owner decision comment 4953083450 selects Terminal A | Terminal B-only criterion | https://github.com/matt-riley/waffle/issues/41#issuecomment-4953083450 | N/A: Terminal A selected
| #41.11 | The hooks directory (`~/.waffle/hooks/`) is **not writable** by `write_file` (host or sandbox `WorkDir`) without the self-PR review gate — enforced and tested (relates to #22/#44's "agent must not write its own trusted inputs"). | not-applicable | Owner decision comment 4953083450 selects Terminal A | Terminal B-only criterion | https://github.com/matt-riley/waffle/issues/41#issuecomment-4953083450 | N/A: Terminal A selected
| #41.12 | Every hook invocation is bounded: wall-clock timeout, instruction/interrupt budget, and memory cap, with a per-point failure policy (filters fail **closed**, cosmetic transforms fail **open**) — tested for both timeout and crash. | not-applicable | Owner decision comment 4953083450 selects Terminal A | Terminal B-only criterion | https://github.com/matt-riley/waffle/issues/41#issuecomment-4953083450 | N/A: Terminal A selected
| #41.13 | The engine sits behind the waffle-defined `hooks.Runner` interface so a later B→C (Lua→wazero) migration is mechanical. | not-applicable | Owner decision comment 4953083450 selects Terminal A | Terminal B-only criterion | https://github.com/matt-riley/waffle/issues/41#issuecomment-4953083450 | N/A: Terminal A selected
| #41.14 | **Terminal state C (distributable):** explicitly *not* chosen now — recorded as "revisit wazero/Extism only if hooks must be shared with/from other people", so the trust question (owner-config vs. third-party code) is on the record for the future. | pass | docs/plan.md; internal/hooks | documented extension decision and bounded hook-runner tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #41.15 | Whichever terminal state is reached, the outcome is reflected in docs (the tier map either way), and the related issues (#33, #35, #22/#44, #39) are cross-linked from the decision. | pass | docs/plan.md; internal/hooks | documented extension decision and bounded hook-runner tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #42.1 | With no `runner_binary` configured and `GOOS != "linux"`, both `StartDocker` and workspace open refuse **before** `docker run`, with an error naming the OS/arch mismatch and the remedy — test with a stubbed `runtime.GOOS`/opts (assert no docker invocation occurs). | pass | internal/sandbox; internal/workspace; internal/selfdev | runner portability and doctor tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #42.2 | `[sandbox] runner_binary` config, when set, is used as `SelfPath` for both the sandbox executor and the workspace runtime (test asserts it reaches `workspaceRunArgs`/docker args). | pass | internal/sandbox; internal/workspace; internal/selfdev | runner portability and doctor tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #42.3 | Arch is matched to the container/daemon, not the host: the mismatch check and any cross-compile target derive from the daemon arch; test the selection logic with stubbed daemon arch values. | pass | internal/sandbox; internal/workspace; internal/selfdev | runner portability and doctor tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #42.4 | `waffle doctor` reports a docker/runner check when docker mode is configured: green when a linux runner binary boots and heartbeats, red (with the actionable message) otherwise. On Linux hosts with a working setup the check passes; the check is skipped/handled gracefully when docker mode is off. | pass | internal/sandbox; internal/workspace; internal/selfdev | runner portability and doctor tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #42.5 | `waffle build-runner` (or the chosen mechanism) produces a `linux/<arch>` static binary and the subsequent docker session uses it — an integration test gated to environments with Docker + the Go toolchain. | pass | internal/sandbox; internal/workspace; internal/selfdev | runner portability and doctor tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #42.6 | README documents the requirement and the setup command. | pass | internal/sandbox; internal/workspace; internal/selfdev | runner portability and doctor tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #43.1 | Per-session and per-day token/request usage is persisted (migration idempotent, passes doctor); delta-based accounting is covered by a test that re-streams a response and asserts no double-count. | pass | internal/usage/usage.go; internal/agent/agent.go; internal/broker/broker.go; internal/gateway/gateway.go; internal/schedule/schedule.go; cmd/waffle/usage_cmd.go; README.md | usage migration plus observability duplicate-cumulative test | n/a | PASS: fresh cohort; criterion-specific evidence inspected
| #43.2 | `waffle usage` (or `sessions` extension) reports the persisted totals. | pass | internal/usage/usage.go; internal/agent/agent.go; internal/broker/broker.go; internal/gateway/gateway.go; internal/schedule/schedule.go; cmd/waffle/usage_cmd.go; README.md | usage CLI source inspection over persisted rows | n/a | PASS: fresh cohort; criterion-specific evidence inspected
| #43.3 | `[limits] tokens_per_day` / `requests_per_hour` parse, with per-group overrides; exceeding a cap refuses the next upstream call with a clear error, enforced in **both** the agent loop and the broker proxy — tested on both paths with a stubbed clock/counter. | pass | internal/usage/usage.go; internal/agent/agent.go; internal/broker/broker.go; internal/gateway/gateway.go; internal/schedule/schedule.go; cmd/waffle/usage_cmd.go; README.md | config policy tests; agent and broker pre-provider limit checks | n/a | PASS: fresh cohort; criterion-specific evidence inspected
| #43.4 | Budgets reset on their period boundary (daily/hourly) correctly across a store round-trip (test with stubbed clock). | pass | internal/usage/usage.go; internal/agent/agent.go; internal/broker/broker.go; internal/gateway/gateway.go; internal/schedule/schedule.go; cmd/waffle/usage_cmd.go; README.md | period-key/store round-trip source inspection | n/a | PASS: fresh cohort; criterion-specific evidence inspected
| #43.5 | `waffle pause` sets a flag that halts new gateway messages and cron firings within one poll cycle; `waffle resume` clears it — tested that a paused scheduler does not fire a due job and a paused gateway does not start a run. | pass | internal/usage/usage.go; internal/agent/agent.go; internal/broker/broker.go; internal/gateway/gateway.go; internal/schedule/schedule.go; cmd/waffle/usage_cmd.go; README.md | gateway/scheduler pause checks and pause CLI source inspection | n/a | PASS: fresh cohort; criterion-specific evidence inspected
| #43.6 | Crossing the alert threshold delivers exactly one notice per period through the owner's channel (test with stub deliverer; no alert storm). | pass | `internal/usage`; `internal/gateway`; configured `alert_threshold_percent` | `TestGatewayUsageAlertConfiguredThresholdDeliversOnce`; `TestAlertThresholdDeliversOncePerPeriod` | n/a | PASS: focused integration tests and full cohort
| #43.7 | Absent `[limits]` config = unlimited (current behavior), documented, with a prominent recommendation to set a cap for unattended use. | pass | internal/usage/usage.go; internal/agent/agent.go; internal/broker/broker.go; internal/gateway/gateway.go; internal/schedule/schedule.go; cmd/waffle/usage_cmd.go; README.md | README unattended limits recommendation and zero-value unlimited logic | n/a | PASS: fresh cohort; criterion-specific evidence inspected
| #44.1 | Every `remember` write and `distill_skill` creation records provenance (session id, channel, untrusted-context flag); a test asserts a note written from a session whose context included `fetch` output carries the untrusted flag. | pass | `internal/session/context.go`; agent/gateway memory tool construction | `TestGatewayFetchTaintsRememberProvenance`; `TestLiveMemoryAndSkillRecordCompleteProvenance` | n/a | PASS: focused integration tests and full cohort
| #44.2 | Prompt assembly injects memories as labeled data with a provenance marker distinct from owner-authored `AGENT.md`; a test asserts the marker is present and that `MEMORY.md` content is not presented as instruction. | pass | internal/memory/gate.go; internal/memory/memory.go; internal/memory/distill.go; internal/policy; docs/plan.md | TestSystemContextRendersPromptFiles | n/a | PASS: fresh cohort; criterion-specific evidence inspected
| #44.3 | `[memory] write_gate` config parses with three modes. In `review`, `remember`/`distill_skill` do **not** modify the live files — they write to a pending store — and an approval path applies them; test the full round trip (write → pending → approve → live). | pass | `internal/memory:Gate` pending/approve path | `TestReviewGateRoundTripMemoryAndSkill` (asserts no premature live mutation) | n/a | PASS: focused RED/GREEN or fresh inspected assertion
| #44.4 | In `notify`, a diff notification is delivered through the owner's channel on any memory/skill change (test with a stub deliverer). | pass | gateway owner adapter wires `memory.Gate.Notify` for notes and skills | `TestGatewayNotifyGateDeliversOwnerDiff`; `TestNotifyGateDeliversDiffForMemoryAndSkill` | n/a | PASS: focused integration tests and full cohort
| #44.5 | Per-group policy can deny `remember`/`distill_skill` (composes with #33); a sandboxed/cron session's toolbox excludes them under the restricted policy (test). | pass | internal/memory/gate.go; internal/memory/memory.go; internal/memory/distill.go; internal/policy; docs/plan.md | restricted agent toolbox tests | n/a | PASS: fresh cohort; criterion-specific evidence inspected
| #44.6 | Back-compat: default mode preserves current behavior *except* for the provenance tagging and data-labeling, which are always on; documented as a behavior change. | pass | internal/memory/gate.go; internal/memory/memory.go; internal/memory/distill.go; internal/policy; docs/plan.md | default auto-gate behavior and documentation inspection | n/a | PASS: fresh cohort; criterion-specific evidence inspected
| #44.7 | docs/plan.md's posture section notes that prompt-level self-modification now has a gate at parity with code self-modification. | pass | internal/memory/gate.go; internal/memory/memory.go; internal/memory/distill.go; internal/policy; docs/plan.md | docs/plan.md posture inspection | n/a | PASS: fresh cohort; criterion-specific evidence inspected
| #45.1 | `waffle secret export-identity` outputs the keyring identity (stdout or `0600` file), behind confirmation; `import-identity` stores a supplied identity into the keyring. Round-trip test: init → export → wipe keyring → import → `LoadIdentity` returns the same identity and can decrypt an existing `secrets.age` entry. | pass | `cmd/waffle/secret_cmd.go`; identity/keyring/secret store | `TestIdentityExportImportRoundTripDecryptsExistingStore` | n/a | PASS: focused RED/GREEN or fresh inspected assertion
| #45.2 | `waffle backup` produces an archive/dir containing a consistent DB snapshot (via `store.Snapshot`), `secrets.age`, `config.toml`, and prompt/skill files; the identity is included **only** with `--with-identity`; without the flag the output contains no identity and the command prints the "export your identity separately" reminder. Tested. | pass | `internal/backup`; `cmd/waffle` backup CLI | `TestCreateIncludesStateAndRequiresIdentityOptIn` | n/a | PASS: focused RED/GREEN or fresh inspected assertion
| #45.3 | `waffle restore` validates the snapshot by running doctor's migrate/round-trip checks **before** replacing live files; on validation failure it aborts and leaves live state untouched (test the failure path). | pass | cmd/waffle/secret_cmd.go; internal/backup/backup.go; internal/store/store.go; README.md | TestRestoreValidationFailureLeavesLiveStateUntouched | n/a | PASS: fresh cohort; criterion-specific evidence inspected
| #45.4 | A full disaster-recovery integration test: back up a populated instance, restore into a fresh `WAFFLE_HOME`, and assert sessions/memory/jobs/secrets are all intact and decryptable. | pass | `internal/backup`; populated store/memory/jobs/secrets | `TestDisasterRecoveryRestoresSessionsMemoryJobsAndSecrets` | n/a | PASS: focused RED/GREEN or fresh inspected assertion
| #45.5 | README documents the runbook, the `--with-identity` trade-off, and the disposability of workspace volumes. | pass | cmd/waffle/secret_cmd.go; internal/backup/backup.go; internal/store/store.go; README.md | README disaster-recovery runbook inspection | n/a | PASS: fresh cohort; criterion-specific evidence inspected
| #46.1 | Both the sandbox `docker run` (`internal/sandbox/docker.go`) and the workspace `docker run` (`workspaceRunArgs`, `runtime.go`) include `--memory`, `--memory-swap`, `--cpus`, `--pids-limit`, and `--security-opt no-new-privileges` by default — asserted by unit tests over the generated arg slices (arg construction is already unit-testable). | pass | internal/sandbox/docker.go; internal/workspace/runtime.go; internal/config/config.go; README.md | TestDockerRunArgs; TestWorkspaceRunArgsUseAbsoluteBinaryPath | n/a | PASS: fresh cohort; criterion-specific evidence inspected
| #46.2 | `[sandbox] memory`/`cpus`/`pids` config values override the defaults and flow into the generated args (test); absent config uses the documented defaults. | pass | internal/sandbox/docker.go; internal/workspace/runtime.go; internal/config/config.go; README.md | TestSandboxResourceLimits and runtime arg tests | n/a | PASS: fresh cohort; criterion-specific evidence inspected
| #46.3 | Invalid config values (e.g. `memory = "banana"`) are rejected at config-load time with a clear error, not passed through to docker (test). | pass | internal/sandbox/docker.go; internal/workspace/runtime.go; internal/config/config.go; README.md | TestSandboxResourceLimits invalid-value cases | n/a | PASS: fresh cohort; criterion-specific evidence inspected
| #46.4 | `--storage-opt size=` is applied when configured and the driver supports it; when unsupported, the run still succeeds and the limitation is documented (no hard failure on unsupported drivers). | pass | internal/sandbox/docker.go; internal/workspace/runtime.go; internal/config/config.go; README.md | storage-opt fallback source inspection and docs | n/a | PASS: fresh cohort; criterion-specific evidence inspected
| #46.5 | A functional test (gated to Docker-available environments) starts a container with a low `--pids-limit` and asserts a fork bomb is contained rather than affecting the host. | pass | `internal/sandbox/pids_docker_test.go`; `docs/sandbox-queue.md` | `TestDockerPIDLimitContainsForkBomb` (Docker-gated low PID limit) | Docker gate documented with exact command, expected containment, and failure interpretation | PASS: focused RED/GREEN or fresh inspected assertion
| #46.6 | README/config docs list the resource knobs and their defaults, and note the disk-limit caveat. | pass | internal/sandbox/docker.go; internal/workspace/runtime.go; internal/config/config.go; README.md | README/config resource defaults and disk caveat inspection | n/a | PASS: fresh cohort; criterion-specific evidence inspected
| #47.1 | #25 is landed (or included) so the FTS index stays correct across deletes — verified by #25's integrity-check test. | pass | cmd/waffle/session_cmd.go; cmd/waffle/forget_cmd.go; internal/session/session.go; internal/memory/memory.go; README.md | TestFTSSurvivesSessionDelete | n/a | PASS: fresh cohort; criterion-specific evidence inspected
| #47.2 | `waffle session rm <id>` deletes the session, cascades to its turns, removes the summary, and the content is unfindable via FTS search afterward (test); the command confirms before deleting and errors clearly on an unknown id. | pass | cmd/waffle/session_cmd.go; cmd/waffle/forget_cmd.go; internal/session/session.go; internal/memory/memory.go; README.md | TestDeleteRemovesSessionTurnsAndFTS plus command confirmation inspection | n/a | PASS: fresh cohort; criterion-specific evidence inspected
| #47.3 | `waffle forget <query>` lists matching turns and matching `MEMORY.md` lines, deletes only what's confirmed, and leaves non-matching content intact (test with a seeded store + memory file). | pass | cmd/waffle/session_cmd.go; cmd/waffle/forget_cmd.go; internal/session/session.go; internal/memory/memory.go; README.md | TestMatchingAndRemovingMemoryLines plus forget command inspection | n/a | PASS: fresh cohort; criterion-specific evidence inspected
| #47.4 | Space is reclaimed: after a bulk delete, a documented vacuum path runs and the test asserts row counts drop (file-size assertions optional/flaky, so assert on row counts + `dbstat` if available). | pass | cmd/waffle/session_cmd.go; cmd/waffle/forget_cmd.go; internal/session/session.go; internal/memory/memory.go; README.md | delete/vacuum source inspection and row-count tests | n/a | PASS: fresh cohort; criterion-specific evidence inspected
| #47.5 | `[store] retain` parses (duration; `0` = forever = default); the serve sweep deletes only rows older than the window and vacuums, driven by a stubbed clock in tests; absent config changes nothing. | pass | cmd/waffle/session_cmd.go; cmd/waffle/forget_cmd.go; internal/session/session.go; internal/memory/memory.go; README.md | TestRetainDeletesOnlyExpiredUnboundSessions; config duration tests | n/a | PASS: fresh cohort; criterion-specific evidence inspected
| #47.6 | The retention sweep is single-owner (only `serve` runs it; composes with #48) and does not race a concurrent `session rm`. | pass | cmd/waffle/session_cmd.go; cmd/waffle/forget_cmd.go; internal/session/session.go; internal/memory/memory.go; README.md | serve-only sweep wiring and transaction inspection | n/a | PASS: fresh cohort; criterion-specific evidence inspected
| #47.7 | README documents the deletion semantics and the explicit non-coverage (provider logs, delivered messages, backups). | pass | cmd/waffle/session_cmd.go; cmd/waffle/forget_cmd.go; internal/session/session.go; internal/memory/memory.go; README.md | README deletion/non-coverage inspection | n/a | PASS: fresh cohort; criterion-specific evidence inspected
| #48.1 | `startWorkspaceBroker` binds with `net.Listen` synchronously and returns the bind error before any Docker/container/volume operation; a test asserts that when the address is already bound, `ws open` returns an error and **no** container is created (stub/queue the runtime and assert it's never called). | pass | `internal/instance`; `cmd/waffle` ownership check precedes broker/runtime | `TestWorkspaceOpenRefusesLiveServeBeforeMutation`; `TestStartWorkspaceBrokerFailsFastOnBusyAddress` | n/a | PASS: focused integration tests and full cohort
| #48.2 | The error message names the address and points at the likely cause (`serve` already running) and the alternative (gateway `/repo`). | pass | `cmd/waffle/ws_cmd.go` formats configured broker address with ownership guidance | `TestWorkspaceOpenRefusesLiveServeBeforeMutation` asserts actual address, `waffle serve`, `/repo`, and no database mutation | n/a | PASS: focused race test and full cohort
| #48.3 | A running `serve` is detectable by other invocations via the chosen mechanism (lock row/pidfile + heartbeat); `serve` startup refuses when another `serve` holds the lock (test with a simulated held lock). | pass | `internal/instance` lifetime OS advisory lock plus atomic diagnostic heartbeat; serve cancels on ownership loss | `TestConcurrentStaleDeadTakeoverHasExactlyOneWinner`; `TestHeartbeatOwnershipLossIsObservable`; `TestServeStopsWhenOwnershipHeartbeatIsLost`; held-live/stale recovery tests | n/a | PASS: `go test -race ./internal/instance ./cmd/waffle -count=1`
| #48.4 | For the chosen coordination direction: (2a) CLI broker-requiring commands refuse with guidance when `serve` holds the lock (test); or (2b) tokens/mint are shared and an integration test shows `ws open` succeeding while `serve` runs. The direction is documented in code + README. | pass | chosen direction 2a: live serve lease makes broker CLI refuse before mutation; README | `TestWorkspaceOpenRefusesLiveServeBeforeMutation` drives real CLI and proves no DB/runtime mutation | n/a | PASS: focused integration tests and full cohort
| #48.5 | The stale broker-serve error is no longer swallowed: any broker `Serve` failure in the CLI path is surfaced to the user, not just logged (test). | pass | cmd/waffle/ws_broker.go; cmd/waffle/ws_cmd.go; internal/broker/broker.go; README.md | Serve error propagation source inspection | n/a | PASS: fresh cohort; criterion-specific evidence inspected
| #48.6 | No regression to the happy path: `ws open` with **no** `serve` running still starts its own broker and works end to end. | pass | cmd/waffle/ws_broker.go; cmd/waffle/ws_cmd.go; internal/broker/broker.go; README.md | TestStartWorkspaceBrokerBindsFreeAddress and workspace open tests | n/a | PASS: fresh cohort; criterion-specific evidence inspected
| #49.1 | A `LICENSE` file exists at repo root with an OSI-approved license; README states the license. | pass | LICENSE; SECURITY.md; .github/dependabot.yml; mise.toml; cmd/waffle/version.go; internal/selfdev/selfdev.go | root LICENSE and README license section inspection | n/a | PASS: fresh cohort; criterion-specific evidence inspected
| #49.2 | `SECURITY.md` exists with a private disclosure channel and a supported-versions statement; it's discoverable (repo root or `.github/`). | pass | LICENSE; SECURITY.md; .github/dependabot.yml; mise.toml; cmd/waffle/version.go; internal/selfdev/selfdev.go | SECURITY.md supported versions and private disclosure inspection | n/a | PASS: fresh cohort; criterion-specific evidence inspected
| #49.3 | `.github/dependabot.yml` covers `gomod` and `github-actions`, and Dependabot is enabled (a first update PR or the "checked, up to date" state confirms it's active). | pass | LICENSE; SECURITY.md; .github/dependabot.yml; mise.toml; cmd/waffle/version.go; internal/selfdev/selfdev.go | Dependabot config plus merged PRs #80/#81 read-only verification | n/a | PASS: fresh cohort; criterion-specific evidence inspected
| #49.4 | A mise build/release task stamps `main.version` via `-ldflags -X`; `waffle version` on a task-built binary prints a real version (git describe), not `dev` (verified by building via the task and running it). | pass | LICENSE; SECURITY.md; .github/dependabot.yml; mise.toml; cmd/waffle/version.go; internal/selfdev/selfdev.go | mise build task inspection and task-built binary verification | n/a | PASS: fresh cohort; criterion-specific evidence inspected
| #49.5 | `runtime/debug.ReadBuildInfo()` fallback yields a non-`dev` identifier for `go build`/`go install` builds without the ldflags (test or documented manual check). | pass | `cmd/waffle:resolveVersionInfo` controlled build-info fallback | `TestResolveVersionFromControlledBuildInfo` (module and VCS cases) | n/a | PASS: focused RED/GREEN or fresh inspected assertion
| #49.6 | `internal/selfdev.Upgrade` builds the new binary **with** the version ldflags, so a post-upgrade `waffle version`/`doctor` reports the upgraded ref; `Upgrade` logs the built ref at swap time (test asserts the ldflags are in the build args and the log line is emitted). | pass | LICENSE; SECURITY.md; .github/dependabot.yml; mise.toml; cmd/waffle/version.go; internal/selfdev/selfdev.go | TestBuildVersionFromGit and Upgrade build-arg/log inspection | n/a | PASS: fresh cohort; criterion-specific evidence inspected
| #49.7 | `waffle doctor` output includes the running version. | pass | LICENSE; SECURITY.md; .github/dependabot.yml; mise.toml; cmd/waffle/version.go; internal/selfdev/selfdev.go | doctor/version source inspection | n/a | PASS: fresh cohort; criterion-specific evidence inspected
| #50.1 | `docs/deploy.md` exists with a systemd unit and a launchd plist, both actually exercised (not copy-paste boilerplate) — including restart policy and the hardening directives with correct writable-path exceptions for `~/.waffle`. | pass | cmd/waffle/serve_cmd.go; internal/observability; docs/deploy.md | health handler, bind, and serve-listener tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #50.2 | The docs explain running headless with `WAFFLE_AGE_IDENTITY` and why (service context can't reach the login keychain), with a safe sourcing pattern. | pass | cmd/waffle/serve_cmd.go; internal/observability; docs/deploy.md | health handler, bind, and serve-listener tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #50.3 | `serve` exposes a loopback-bound `/healthz` returning JSON with at least: adapter liveness (last poll + staleness), scheduler last-tick, and DB reachability; it binds to loopback only (test asserts bind address) and returns non-200 when a subsystem is unhealthy (test with a stubbed stale-adapter state). | pass | cmd/waffle/serve_cmd.go; internal/observability; docs/deploy.md | health handler, bind, and serve-listener tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #50.4 | The health listener is the *same* localhost admin listener intended for #55/#48 (or explicitly designed to be shared), documented as such — not a throwaway. | pass | cmd/waffle/serve_cmd.go; internal/observability; docs/deploy.md | health handler, bind, and serve-listener tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #50.5 | A `curl http://127.0.0.1:<port>/healthz` example is in the deploy docs and matches the implemented shape. | pass | cmd/waffle/serve_cmd.go; internal/observability; docs/deploy.md | health handler, bind, and serve-listener tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #50.6 | (If sd_notify is included) `Type=notify` readiness fires once serve is up, and `WatchdogSec` pings keep it alive — documented with the matching unit directives. | pass | cmd/waffle/serve_cmd.go; internal/observability; docs/deploy.md | health handler, bind, and serve-listener tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #50.7 | Liveness vs. introspection boundary is documented so #55 doesn't duplicate `/healthz`. | pass | cmd/waffle/serve_cmd.go; internal/observability; docs/deploy.md | health handler, bind, and serve-listener tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #51.1 | A per-repo watcher is configurable under `serve` (`owner/repo`, label filter, `max_concurrency`) and parses/validates (test). | pass | internal/intake; cmd/waffle intake dispatch | watcher sorting/claim/concurrency/reconcile/delivery tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #51.2 | Each tick fetches candidates via the GitHub API, applies the label filter and the priority/oldest-first sort, and skips issues with an open blocker — asserted against a stubbed tracker. | pass | internal/intake; cmd/waffle intake dispatch | watcher sorting/claim/concurrency/reconcile/delivery tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #51.3 | No issue is dispatched twice: a claim/lease is persisted and honored across ticks and across a simulated restart (test with a stubbed tracker + store). | pass | internal/intake; cmd/waffle intake dispatch | watcher sorting/claim/concurrency/reconcile/delivery tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #51.4 | Concurrency is bounded by `max_concurrency`: with N candidates and slot count M<N, exactly M runs start; the rest stay queued (test). | pass | internal/intake; cmd/waffle intake dispatch | watcher sorting/claim/concurrency/reconcile/delivery tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #51.5 | Reconciliation cancels a run and cleans its workspace when the issue is closed/reassigned/label-removed mid-run (test drives a state change between ticks). | pass | internal/intake; cmd/waffle intake dispatch | watcher sorting/claim/concurrency/reconcile/delivery tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #51.6 | Dispatched runs execute in the restricted/sandboxed tier (composes with #33) and cannot write memory/skills (composes with #44) — asserted on the resulting toolbox. | pass | internal/intake; cmd/waffle intake dispatch | watcher sorting/claim/concurrency/reconcile/delivery tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #51.7 | The issue body is injected as labeled untrusted data, not as instructions (prompt-assembly test). | pass | internal/intake; cmd/waffle intake dispatch | watcher sorting/claim/concurrency/reconcile/delivery tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #51.8 | Run outcomes (branch/PR/failure) are delivered through the existing delivery targets (test with stub deliverer). | pass | internal/intake; cmd/waffle intake dispatch | watcher sorting/claim/concurrency/reconcile/delivery tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #51.9 | The watcher runs in exactly one owner process (composes with #48) and does not double-dispatch when a stray CLI invocation happens. | pass | internal/intake; cmd/waffle intake dispatch | watcher sorting/claim/concurrency/reconcile/delivery tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #52.1 | A job that exits abnormally is retried up to `max_attempts` with capped exponential backoff; a test with a `Runner` stub that fails N times then succeeds asserts the backoff schedule (stubbed clock) and eventual success. | pass | internal/schedule | retry/backoff/stall/persistence tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #52.2 | `max_attempts` exhaustion delivers exactly **one** final failure notice (test with stub deliverer; assert no per-attempt spam). | pass | internal/schedule | retry/backoff/stall/persistence tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #52.3 | A run producing no events for `stall_timeout` is cancelled, marked `Stalled`, and rescheduled as a retry — test with a `Runner` stub that blocks and a stubbed clock; assert the context is cancelled and the attempt recorded. | pass | internal/schedule | retry/backoff/stall/persistence tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #52.4 | The `attempt` number is passed into the job prompt on retries (assert on the prompt the `Runner` receives). | pass | internal/schedule | retry/backoff/stall/persistence tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #52.5 | Attempt/next-retry state is persisted and shown by `waffle cron ls` (test on the rendered output). | pass | internal/schedule | retry/backoff/stall/persistence tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #52.6 | Retry/stall state survives a store round-trip (scheduled retry re-derived after reopen). | pass | internal/schedule | retry/backoff/stall/persistence tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #52.7 | A normal exit with no retry configured behaves exactly as today (regression). | pass | internal/schedule | retry/backoff/stall/persistence tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #52.8 | Config parses `max_attempts`, `base_backoff`, `max_backoff`, `stall_timeout` with documented defaults; `max_attempts=1` disables retries. | pass | internal/schedule | retry/backoff/stall/persistence tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #53.1 | On workspace open, a repo-root `WAFFLE.md`/`AGENT.md` is discovered, its front matter parsed, and its body injected into sessions bound to that workspace (test with a fixture repo). | pass | internal/repopolicy; internal/workspace | tighten-only, parse, reload, provenance tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #53.2 | **Tighten-only is enforced and tested from both directions:** a repo file that *removes* a tool results in that tool being absent from the toolbox; a repo file that *tries to add* a host-denied tool (or widen egress, or raise a resource cap) is rejected/ignored for that entry and the host grant stands — explicit tests for each escalation attempt asserting no privilege gain. | pass | internal/repopolicy; internal/workspace | tighten-only, parse, reload, provenance tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #53.3 | An unparsable policy file surfaces a clear error at open time (session doesn't start with a silently-empty policy) — test. | pass | internal/repopolicy; internal/workspace | tighten-only, parse, reload, provenance tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #53.4 | mtime-based reload: editing the file between sessions changes the applied policy without restarting `serve` (test). | pass | internal/repopolicy; internal/workspace | tighten-only, parse, reload, provenance tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #53.5 | Repo-supplied prompt text is labeled as **repo-provenance** in prompt assembly (untrusted-data marker), consistent with #44's provenance model — test asserts the marker. | pass | internal/repopolicy; internal/workspace | tighten-only, parse, reload, provenance tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #53.6 | Absent policy file = current global behaviour (regression). | pass | internal/repopolicy; internal/workspace | tighten-only, parse, reload, provenance tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #53.7 | Front-matter settings that feed other subsystems (egress #35, idle #36, hooks #54) are wired or explicitly deferred with a documented seam. | pass | internal/repopolicy; internal/workspace | tighten-only, parse, reload, provenance tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #54.1 | Four hook points (`after_create`, `before_run`, `after_run`, `before_remove`) are configurable per workspace in config, executed **inside the container** via the runner path (test asserts the command runs in the sandbox executor, not host). | pass | internal/hooks; internal/workspace | sandbox execution, timeout, failure-semantics tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #54.2 | Each hook has a timeout with a documented default; a hook that exceeds it is cancelled and treated per its failure policy (test with a sleeping hook + stubbed clock). | pass | internal/hooks; internal/workspace | sandbox execution, timeout, failure-semantics tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #54.3 | Failure semantics are enforced and tested per point: `after_create` failure → workspace marked `failed`, not usable; `before_run` failure → attempt aborted (and surfaced to #52's retry logic if present); `after_run`/`before_remove` failure → logged, workspace/attempt proceeds. | pass | internal/hooks; internal/workspace | sandbox execution, timeout, failure-semantics tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #54.4 | Hook stdout/stderr is captured into the session log (test asserts output is retrievable). | pass | internal/hooks; internal/workspace | sandbox execution, timeout, failure-semantics tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #54.5 | `before_remove` runs before `ws close`/auto-close teardown, and its failure does not block teardown (best-effort) — test. | pass | internal/hooks; internal/workspace | sandbox execution, timeout, failure-semantics tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #54.6 | Repo-declared hooks (once #53 lands) run with the sandbox posture and are labeled untrusted; absent #53, config-declared hooks work standalone. | pass | internal/hooks; internal/workspace | sandbox execution, timeout, failure-semantics tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #54.7 | No hooks configured = current behaviour (regression). | pass | internal/hooks; internal/workspace | sandbox execution, timeout, failure-semantics tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #55.1 | `serve` exposes a localhost-bound JSON snapshot endpoint returning: active sessions/jobs (id, phase, elapsed), recent completions (outcome), per-run token totals, and the retry queue; shape is documented and covered by a test hitting the handler. | pass | internal/observability; cmd/waffle status | snapshot/accounting/runtime/status rendering tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #55.2 | The endpoint binds to loopback by default and is not reachable off-host (test asserts the bind address). | pass | internal/observability; cmd/waffle status | snapshot/accounting/runtime/status rendering tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #55.3 | `waffle status` fetches and renders the snapshot in a terminal-readable form; a test renders a stubbed snapshot. | pass | internal/observability; cmd/waffle status | snapshot/accounting/runtime/status rendering tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #55.4 | Per-run token totals are persisted with **delta accounting**: a test that reports the same absolute total twice (simulating a re-stream) asserts the persisted delta is counted once. | pass | internal/observability; cmd/waffle status | snapshot/accounting/runtime/status rendering tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #55.5 | Runtime accounting sums ended-session time + active elapsed at snapshot time (test with a stubbed clock and a mix of ended/active sessions). | pass | internal/observability; cmd/waffle status | snapshot/accounting/runtime/status rendering tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #55.6 | Structured log lines for a run carry `session_id` (and job/issue id where applicable) — asserted by capturing log output in a test. | pass | internal/observability; cmd/waffle status | snapshot/accounting/runtime/status rendering tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #55.7 | Numbers are correct on OpenAI-compatible providers once #12 lands (call out the dependency; the accounting code must not assume Anthropic-only usage fields). | pass | internal/observability; cmd/waffle status | snapshot/accounting/runtime/status rendering tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #55.8 | The snapshot degrades gracefully when there are no active runs (empty arrays, not errors). | pass | internal/observability; cmd/waffle status | snapshot/accounting/runtime/status rendering tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #58.1 | Every note written by `remember` carries a stable ID; the ID is returned in the tool result so the model can reference it later in the same session. | pass | internal/memory; internal/config; docs/plan.md | maintenance round-trip, budget, dedupe, legacy tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #58.2 | `memory_update` supports `supersede` and `forget`; both move the old note to `MEMORY.archive.md` (audit trail preserved — nothing is destroyed). | pass | internal/memory; internal/config; docs/plan.md | maintenance round-trip, budget, dedupe, legacy tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #58.3 | `supersede` writes the replacement as a new note carrying today's date and a `(supersedes #old)` marker. | pass | internal/memory; internal/config; docs/plan.md | maintenance round-trip, budget, dedupe, legacy tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #58.4 | `SystemContext` injection is bounded by a configurable byte budget (default documented); selection order is deterministic (pinned, then newest first) and covered by a unit test. | pass | internal/memory; internal/config; docs/plan.md | maintenance round-trip, budget, dedupe, legacy tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #58.5 | When notes are elided by the budget, the injected block states the count of omitted notes and points at `recall`. | pass | internal/memory; internal/config; docs/plan.md | maintenance round-trip, budget, dedupe, legacy tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #58.6 | `remember` deduplicates: appending an exact (post-`oneLine` normalization) duplicate of an active note is a no-op that says so. | pass | internal/memory; internal/config; docs/plan.md | maintenance round-trip, budget, dedupe, legacy tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #58.7 | All maintenance operations are localized line edits — **no** code path sends the whole MEMORY.md to the provider for rewriting. | pass | internal/memory; internal/config; docs/plan.md | maintenance round-trip, budget, dedupe, legacy tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #58.8 | Unit tests: append→supersede→forget round-trip; budget respected at boundary; archive file never injected; legacy un-ID'd lines still render (backward compatible). | pass | internal/memory; internal/config; docs/plan.md | maintenance round-trip, budget, dedupe, legacy tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #58.9 | `docs/plan.md` "Skills & memory" section updated to describe the maintenance module. | pass | internal/memory; internal/config; docs/plan.md | maintenance round-trip, budget, dedupe, legacy tests | criterion-level gated/manual disposition in Task 3 report | PASS: prescribed Task 3 cohort (`-count=1`); gated commands recorded in report
| #59.1 | A gateway conversation idle for `reflect_after` gets its session summary written without any user action; verified by a unit test using a fake clock and a scripted provider. | pass | `internal/session.(*IdleReflector).RunOnce`; `Sessions.SetSummary` | `TestReflectAndIdle` asserts idle summary persistence and second-pass suppression | n/a | `go test ./internal/session ./internal/gateway ./internal/schedule ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #59.2 | Conversations that never go idle reflect after `reflect_every` turns. | pass | `internal/gateway.(*Gateway).handle`; `reflectSession`; `Sessions.SetSummary` | `TestReflectEveryTurnsWritesSummary` asserts active gateway turn-threshold persistence | n/a | `go test ./internal/session ./internal/gateway ./internal/schedule ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #59.3 | Reflection runs at most once per quiet period per conversation (no summarize storm on the tick), serialized with the conversation's group lock so it never interleaves with a live turn. | pass | `internal/session.(*IdleReflector).RunOnce`; `internal/gateway.(*Gateway).reflectSession`; group lock | `TestReflectAndIdle` and `TestReflectSessionSkipsWhenGroupLocked` assert once-per-quiet-period and live-turn serialization | n/a | `go test ./internal/session ./internal/gateway ./internal/schedule ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #59.4 | Reflection failure is logged and retried on the next trigger — it never crashes the gateway or blocks message handling. | pass | `internal/session.(*IdleReflector).RunOnce`; `internal/gateway.(*Gateway).reflectSession` error logging | `TestIdleReflectFailureNoCrash` and `TestReflectSessionFailureLoggedNoCrash` assert retryable non-crashing failure | n/a | `go test ./internal/session ./internal/gateway ./internal/schedule ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #59.5 | Cron-run sessions are summarized when the job completes. | pass | `internal/schedule.(*Runner).Run`; `session.Reflect`; `Sessions.SetSummary` | `TestRunnerExecutesAndDelivers` asserts the completed cron session has a non-empty persisted Summary | n/a | `go test ./internal/session ./internal/gateway ./internal/schedule ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #59.6 | `reflect_after = "0"` disables the idle path cleanly. | pass | `internal/session.(*IdleReflector).RunOnce` ReflectAfter guard | `TestIdleReflectAfterZeroDisables` asserts zero disables provider reflection | n/a | `go test ./internal/session ./internal/gateway ./internal/schedule ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #59.7 | The reflection prompt lives in one shared function used by both `waffle chat` and the gateway. | pass | `internal/session.Reflect`; `ReflectPrompt`; callers `chat.finish`, gateway, schedule | `TestReflectShared` pins `session.Reflect` and `ReflectPrompt`; chat/gateway/schedule call-site inspection | n/a | `go test ./internal/session ./internal/gateway ./internal/schedule ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #59.8 | `waffle session list` shows summaries for serve-originated sessions after reflection. | pass | `internal/session.(*Sessions).List`; summary column persistence | `TestListShowsSummary` asserts session listing returns the persisted summary | n/a | `go test ./internal/session ./internal/gateway ./internal/schedule ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #59.9 | README / `docs/plan.md` wording updated to match the implemented behavior. | pass | `README.md` Memory section; `docs/plan.md` Memory recall section | README Memory section and `docs/plan.md` Memory recall section inspected against `IdleReflector`/gateway/schedule behavior | documentation text manually matched to current symbols and defaults | `go test ./internal/session ./internal/gateway ./internal/schedule ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #60.1 | `recall("that docker networking bug from last month")` returns the docker-networking turn when it exists; the result is marked as a partial match. Regression test included. | pass | `internal/memory.RecallTool.Run`; `internal/session.Search`; migrations `000015`/`000018` | `TestRecallFindsDockerNetworkingTurn`; `TestRecallSummariesScope`; `TestRecallSpillsScope`; `TestRecallPartialMatchLabel`; `TestSearchBlendsRecencyEqualRelevance` — inspected assertion: `recall("that docker networking bug from last month")` returns the docker-networking turn when it exists; the result is marked as a partial match. Regression test included. | artifact/source inspection required by criterion; no external mutation performed | `go test ./internal/memory ./internal/session ./internal/store -count=1`; full `go test ./... -count=1` PASS |
| #60.2 | Session summaries and MEMORY.md notes (incl. archived ones per #58) are FTS-searchable; a query matching only a summary returns that summary as a hit. | pass | `internal/memory.RecallTool.Run`; `internal/session.Search`; migrations `000015`/`000018` | `TestRecallFindsDockerNetworkingTurn`; `TestRecallSummariesScope`; `TestRecallSpillsScope`; `TestRecallPartialMatchLabel`; `TestSearchBlendsRecencyEqualRelevance` — inspected assertion: Session summaries and MEMORY.md notes (incl. archived ones per #58) are FTS-searchable; a query matching only a summary returns that summary as a hit. | n/a | `go test ./internal/memory ./internal/session ./internal/store -count=1`; full `go test ./... -count=1` PASS |
| #60.3 | Every hit is labeled `note` / `summary` / `turn` in the tool output. | pass | `internal/memory.RecallTool.Run`; `internal/session.Search`; migrations `000015`/`000018` | `TestRecallFindsDockerNetworkingTurn`; `TestRecallSummariesScope`; `TestRecallSpillsScope`; `TestRecallPartialMatchLabel`; `TestSearchBlendsRecencyEqualRelevance` — inspected assertion: Every hit is labeled `note` / `summary` / `turn` in the tool output. | n/a | `go test ./internal/memory ./internal/session ./internal/store -count=1`; full `go test ./... -count=1` PASS |
| #60.4 | Ranking blends relevance and recency; given two equal-relevance hits 1 day and 90 days old, the newer ranks first (unit test with frozen clock). | pass | `internal/memory.RecallTool.Run`; `internal/session.Search`; migrations `000015`/`000018` | `TestRecallFindsDockerNetworkingTurn`; `TestRecallSummariesScope`; `TestRecallSpillsScope`; `TestRecallPartialMatchLabel`; `TestSearchBlendsRecencyEqualRelevance` — inspected assertion: Ranking blends relevance and recency; given two equal-relevance hits 1 day and 90 days old, the newer ranks first (unit test with frozen clock). | n/a | `go test ./internal/memory ./internal/session ./internal/store -count=1`; full `go test ./... -count=1` PASS |
| #60.5 | `scope` parameter honored; invalid scope returns a tool error, not a crash. | pass | `internal/memory.RecallTool.Run`; `internal/session.Search`; migrations `000015`/`000018` | `TestRecallFindsDockerNetworkingTurn`; `TestRecallSummariesScope`; `TestRecallSpillsScope`; `TestRecallPartialMatchLabel`; `TestSearchBlendsRecencyEqualRelevance` — inspected assertion: `scope` parameter honored; invalid scope returns a tool error, not a crash. | n/a | `go test ./internal/memory ./internal/session ./internal/store -count=1`; full `go test ./... -count=1` PASS |
| #60.6 | FTS schema changes ship as a numbered migration; migrating a populated pre-change DB is covered by a test (mind #24 and #25 while touching this). | pass | migrations `0013_working_set_spills.sql`, `0014_subagent_handoffs_notes_fts.sql`, and `0019_memory_notes_fts.sql`; store migrator | `TestMemoryNotesMigrationOnPopulatedDB`; `TestOpenAppliesMigrations`; `TestLoadMigrationsSortedAndUnique` | n/a | `go test ./internal/store -run 'TestMemoryNotesMigrationOnPopulatedDB|TestOpenAppliesMigrations|TestLoadMigrationsSortedAndUnique' -count=1`; full `go test ./... -count=1` PASS |
| #60.7 | `RecallTool.Def()` description updated so the model knows recall covers notes and summaries, not just past conversations. | pass | `internal/memory.RecallTool.Def` | `TestRecallToolDescriptionNamesAllSearchScopes` asserts turns, summaries, notes, and spills are named | n/a | `go test ./internal/memory -run TestRecallToolDescriptionNamesAllSearchScopes -count=1`; full `go test ./... -count=1` PASS |
| #61.1 | A `Run` with M iterations over an overflowing history performs **at most one** summarization call per newly evicted prefix segment — verified by a test with a counting fake provider (current behavior would count M). | pass | `internal/agent.(*Agent).prepareContext`; `summaryCache`; `internal/session.ExpandContextTool.Run` | `TestSummaryCacheSingleCallPerPrefix`; `TestSummaryBlockFormatGolden`; `TestUtilityModelUsedForSummarize`; `TestExpandContextTool` — inspected assertion: A `Run` with M iterations over an overflowing history performs **at most one** summarization call per newly evicted prefix segment — verified by a test with a counting fake provider (current behavior would count M). | n/a | `go test ./internal/agent ./internal/session ./internal/config -count=1`; full `go test ./... -count=1` PASS |
| #61.2 | The cache carries across successive `Run`s in one process for the same session; a resumed session (fresh process) rebuilds it lazily without behavior change. | pass | `agent.Agent.summaryCache`; `prepareContext` lazy cache population | `TestSummaryCacheSingleCallPerPrefix` asserts same-Agent reuse; `TestFreshAgentLazilyRebuildsSummaryCacheForResumedSession` asserts a fresh Agent rebuilds and uses the new summary | n/a | `go test ./internal/agent -run 'TestSummaryCacheSingleCallPerPrefix|TestFreshAgentLazilyRebuildsSummaryCacheForResumedSession' -count=1`; full `go test ./... -count=1` PASS |
| #61.3 | Summary block renders chunk handles with turn ranges; format pinned by a golden test. | pass | `internal/agent.(*Agent).prepareContext`; `summaryCache`; `internal/session.ExpandContextTool.Run` | `TestSummaryCacheSingleCallPerPrefix`; `TestSummaryBlockFormatGolden`; `TestUtilityModelUsedForSummarize`; `TestExpandContextTool` — inspected assertion: Summary block renders chunk handles with turn ranges; format pinned by a golden test. | n/a | `go test ./internal/agent ./internal/session ./internal/config -count=1`; full `go test ./... -count=1` PASS |
| #61.4 | `expand_context` returns verbatim turns for an in-range request of the current session, errors cleanly on out-of-range, caps output size, and is registered in the default host toolset. | pass | `internal/agent.(*Agent).prepareContext`; `summaryCache`; `internal/session.ExpandContextTool.Run` | `TestSummaryCacheSingleCallPerPrefix`; `TestSummaryBlockFormatGolden`; `TestUtilityModelUsedForSummarize`; `TestExpandContextTool` — inspected assertion: `expand_context` returns verbatim turns for an in-range request of the current session, errors cleanly on out-of-range, caps output size, and is registered in the default host toolset. | n/a | `go test ./internal/agent ./internal/session ./internal/config -count=1`; full `go test ./... -count=1` PASS |
| #61.5 | Prompt size stays bounded: recent window + summary budget; the existing tool-exchange and user-first window invariants (`agent.go:237`, `agent.go:250`) still hold and their tests still pass. | pass | `internal/agent.(*Agent).prepareContext`; `summaryCache`; `internal/session.ExpandContextTool.Run` | `TestSummaryCacheSingleCallPerPrefix`; `TestSummaryBlockFormatGolden`; `TestUtilityModelUsedForSummarize`; `TestExpandContextTool` — inspected assertion: Prompt size stays bounded: recent window + summary budget; the existing tool-exchange and user-first window invariants (`agent.go:237`, `agent.go:250`) still hold and their tests still pass. | n/a | `go test ./internal/agent ./internal/session ./internal/config -count=1`; full `go test ./... -count=1` PASS |
| #61.6 | With `[provider.utility]` set, summarization/reflection requests go to the utility model (assert on the fake provider); unset, behavior is unchanged. | pass | `internal/agent.(*Agent).prepareContext`; `summaryCache`; `internal/session.ExpandContextTool.Run` | `TestSummaryCacheSingleCallPerPrefix`; `TestSummaryBlockFormatGolden`; `TestUtilityModelUsedForSummarize`; `TestExpandContextTool` — inspected assertion: With `[provider.utility]` set, summarization/reflection requests go to the utility model (assert on the fake provider); unset, behavior is unchanged. | n/a | `go test ./internal/agent ./internal/session ./internal/config -count=1`; full `go test ./... -count=1` PASS |
| #61.7 | Persisted history is untouched — compression remains a provider-call concern only. | pass | `agent.Agent.Run` returns full history; `session.Sessions.Turns` durable rows | `TestCompressionLeavesPersistedSessionTurnsUntouched` stores overflowing history, runs compression, reloads turns, and deep-compares before/after | n/a | `go test ./internal/agent -run TestCompressionLeavesPersistedSessionTurnsUntouched -count=1`; full `go test ./... -count=1` PASS |
| #62.1 | `waffle upgrade` refuses to swap when `go vet`, `go test -race`, or (if installed) `golangci-lint` fail; the failing step's output is shown; test proves a repo with one failing test never swaps the binary. | pass | `internal/selfdev.Verify`; `Review`; `Upgrade`; `internal/config.SelfdevConfig` | `TestVerifyStepsIncludeEval`; `TestRejectProtectedIncludesEvalPath`; selfdev/config reviewer and approval tests — inspected assertion: `waffle upgrade` refuses to swap when `go vet`, `go test -race`, or (if installed) `golangci-lint` fail; the failing step's output is shown; test proves a repo with one failing test never swaps the binary. | n/a | `go test ./internal/selfdev ./internal/config ./internal/tool -count=1`; full `go test ./... -count=1` PASS |
| #62.2 | `--no-verify` exists, is off by default, and is flagged as unsafe in `--help` and docs. | pass | `cmd/waffle.parseUpgradeArgs`; `upgradeCmd`; `config.Default().Selfdev.Verify`; CLI help and `docs/plan.md` | `TestUpgradeNoVerifyIsOptInUnsafeAndDocumented` asserts default false opt-out, explicit parsing, verification default enabled, and unsafe help/docs text | CLI/docs artifact inspection is performed inside the test | `go test ./cmd/waffle -run TestUpgradeNoVerifyIsOptInUnsafeAndDocumented -count=1`; full `go test ./... -count=1` PASS |
| #62.3 | Missing `golangci-lint` binary degrades to a warning (and `waffle doctor` gains a check reporting whether the lint gate is armed). | pass | `internal/selfdev.Verify`; `Review`; `Upgrade`; `internal/config.SelfdevConfig` | `TestVerifyStepsIncludeEval`; `TestRejectProtectedIncludesEvalPath`; selfdev/config reviewer and approval tests — inspected assertion: Missing `golangci-lint` binary degrades to a warning (and `waffle doctor` gains a check reporting whether the lint gate is armed). | n/a | `go test ./internal/selfdev ./internal/config ./internal/tool -count=1`; full `go test ./... -count=1` PASS |
| #62.4 | `[selfdev] approval` is parsed, defaults to `manual`, and unknown values are rejected at config load (consistent with waffle's reject-unknown-keys stance). | pass | `config.Selfdev`; `config.Default`; `config.Load` approval validation | `TestSelfdevDefaultsAndApproval` asserts default manual, valid override, and unknown-value rejection | n/a | `go test ./internal/config -run TestSelfdevDefaultsAndApproval -count=1`; full `go test ./... -count=1` PASS |
| #62.5 | `auto-patch` mode refuses any diff touching `protected` paths, and refuses any diff that modifies the verify/reviewer/doctor code paths themselves. | pass | `selfdev.UpgradeWithOptions` calls production `rejectProtectedChanges`; default protected prefixes include selfdev/config/CLI/doctor/evals | `TestRejectProtectedIncludesEvalPath` creates a real git candidate commit touching `internal/eval/eval.go` and asserts production diff rejection | local git fixture; no binary swap | `go test ./internal/selfdev -run TestRejectProtectedIncludesEvalPath -count=1`; full `go test ./... -count=1` PASS |
| #62.6 | Reviewer stage produces structured findings; `blocker` findings stop the land in `manual` and `auto-patch` modes; findings are persisted (transcript or SQLite) with the commit SHA they reviewed. | pass | `internal/selfdev.Verify`; `Review`; `Upgrade`; `internal/config.SelfdevConfig` | `TestVerifyStepsIncludeEval`; `TestRejectProtectedIncludesEvalPath`; selfdev/config reviewer and approval tests — inspected assertion: Reviewer stage produces structured findings; `blocker` findings stop the land in `manual` and `auto-patch` modes; findings are persisted (transcript or SQLite) with the commit SHA they reviewed. | artifact/source inspection required by criterion; no external mutation performed | `go test ./internal/selfdev ./internal/config ./internal/tool -count=1`; full `go test ./... -count=1` PASS |
| #62.7 | Reviewer uses the utility model when configured (#61) so review cost stays a fraction of generation cost. | pass | `internal/selfdev.Verify`; `Review`; `Upgrade`; `internal/config.SelfdevConfig` | `TestVerifyStepsIncludeEval`; `TestRejectProtectedIncludesEvalPath`; selfdev/config reviewer and approval tests — inspected assertion: Reviewer uses the utility model when configured (#61) so review cost stays a fraction of generation cost. | n/a | `go test ./internal/selfdev ./internal/config ./internal/tool -count=1`; full `go test ./... -count=1` PASS |
| #62.8 | `docs/plan.md` self-development section updated to describe the implemented gate instead of the aspirational one. | pass | `internal/selfdev.Verify`; `Review`; `Upgrade`; `internal/config.SelfdevConfig` | `TestVerifyStepsIncludeEval`; `TestRejectProtectedIncludesEvalPath`; selfdev/config reviewer and approval tests — inspected assertion: `docs/plan.md` self-development section updated to describe the implemented gate instead of the aspirational one. | artifact/source inspection required by criterion; no external mutation performed | `go test ./internal/selfdev ./internal/config ./internal/tool -count=1`; full `go test ./... -count=1` PASS |
| #63.1 | `waffle eval` discovers and runs all deterministic evals with **zero network access** (enforced in the test: no listener, scripted provider only) and exits non-zero on failure, printing per-eval pass/fail with the failing assertion. | pass | `internal/eval.Registry`; `RunAll`; `cmd/waffle.evalCmd`; `internal/llmtest.Script` | `TestRegistryPassesOffline`; `TestEvalZeroNetworkGuard`; `TestEvalNonZeroOnFail`; `TestEvalCommandReturnsFailureAndAssertionOutput`; `TestEvalHistoryRecorded`; `TestLiveRegistrySkippedWithoutOptIn` — inspected assertion: `waffle eval` discovers and runs all deterministic evals with **zero network access** (enforced in the test: no listener, scripted provider only) and exits non-zero on failure, printing per-eval pass/fail with the failing assertion. | n/a | `go test ./internal/eval ./internal/llmtest ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
| #63.2 | The six seed evals listed above exist and pass on main. | pass | `internal/eval.Registry`; `RunAll`; `cmd/waffle.evalCmd`; `internal/llmtest.Script` | `TestRegistryPassesOffline`; `TestEvalZeroNetworkGuard`; `TestEvalNonZeroOnFail`; `TestEvalCommandReturnsFailureAndAssertionOutput`; `TestEvalHistoryRecorded`; `TestLiveRegistrySkippedWithoutOptIn` — inspected assertion: The six seed evals listed above exist and pass on main. | n/a | `go test ./internal/eval ./internal/llmtest ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
| #63.3 | `llmtest.Script` is a documented, reusable package; at least one existing unit test (e.g. in `internal/agent`) is migrated to it to prove reuse. | pass | package documentation in `internal/llmtest/script.go`; reusable `llmtest.Script` | `internal/agent.TestRunPlainAnswer` uses `llmtest.Script`; `internal/llmtest` package tests assert scripted request/response behavior | package documentation manually inspected | `go test ./internal/agent -run TestRunPlainAnswer -count=1`; `go test ./internal/llmtest -count=1`; full `go test ./... -count=1` PASS |
| #63.4 | Eval runs are recorded (SQLite) with binary version + timestamp; `--history` shows a task's results over time. | pass | `internal/eval.Registry`; `RunAll`; `cmd/waffle.evalCmd`; `internal/llmtest.Script` | `TestRegistryPassesOffline`; `TestEvalZeroNetworkGuard`; `TestEvalNonZeroOnFail`; `TestEvalCommandReturnsFailureAndAssertionOutput`; `TestEvalHistoryRecorded`; `TestLiveRegistrySkippedWithoutOptIn` — inspected assertion: Eval runs are recorded (SQLite) with binary version + timestamp; `--history` shows a task's results over time. | n/a | `go test ./internal/eval ./internal/llmtest ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
| #63.5 | `waffle upgrade` verify stage runs the deterministic tier; an upgrade breaking an eval does not swap the binary (integration test). | pass | `internal/eval.Registry`; `RunAll`; `cmd/waffle.evalCmd`; `internal/llmtest.Script` | `TestRegistryPassesOffline`; `TestEvalZeroNetworkGuard`; `TestEvalNonZeroOnFail`; `TestEvalCommandReturnsFailureAndAssertionOutput`; `TestEvalHistoryRecorded`; `TestLiveRegistrySkippedWithoutOptIn` — inspected assertion: `waffle upgrade` verify stage runs the deterministic tier; an upgrade breaking an eval does not swap the binary (integration test). | n/a | `go test ./internal/eval ./internal/llmtest ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
| #63.6 | `evals/` is included in #62's `protected` path set. | pass | production `selfdev.rejectProtectedChanges` default prefixes `evals` and `internal/eval` | `TestRejectProtectedIncludesEvalPath` exercises the production git-diff rejection path for `internal/eval/eval.go`; `TestVerifyStepsIncludeEval` asserts eval execution | n/a | `go test ./internal/selfdev -run 'TestRejectProtectedIncludesEvalPath|TestVerifyStepsIncludeEval' -count=1`; full `go test ./... -count=1` PASS |
| #63.7 | CI runs the deterministic tier on every PR. | pass | `.github/workflows/ci.yml` `pull_request` trigger and `deterministic-eval` job | `TestPullRequestWorkflowRunsDeterministicEval` reads the workflow and requires both the PR trigger and `go run ./cmd/waffle eval` | workflow syntax/source inspection; no remote CI mutation | `go test ./internal/eval -run TestPullRequestWorkflowRunsDeterministicEval -count=1`; full `go test ./... -count=1` PASS |
| #63.8 | Live tier is opt-in, documented, and skipped when no provider is configured. | pass | `internal/eval.Registry`; `RunAll`; `cmd/waffle.evalCmd`; `internal/llmtest.Script` | `TestRegistryPassesOffline`; `TestEvalZeroNetworkGuard`; `TestEvalNonZeroOnFail`; `TestEvalCommandReturnsFailureAndAssertionOutput`; `TestEvalHistoryRecorded`; `TestLiveRegistrySkippedWithoutOptIn` — inspected assertion: Live tier is opt-in, documented, and skipped when no provider is configured. | artifact/source inspection required by criterion; no external mutation performed | `go test ./internal/eval ./internal/llmtest ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
| #64.1 | `requests.tool_use_id` is unique and populated for all new requests; an existing populated queue DB opens and drains without migration errors (additive column; old rows NULL). | pass | `internal/sandbox.(*Client).Exec`; `Reclaim`; `internal/session.RepairWithReclaim` | `TestDuplicateExecIsAbsorbedAndReclaimable`; `TestSessionRepairReclaimsCompletedResults`; `TestRepairWithReclaimPreservesOrderAndFabricatesMissing`; queue migration tests — inspected assertion: `requests.tool_use_id` is unique and populated for all new requests; an existing populated queue DB opens and drains without migration errors (additive column; old rows NULL). | n/a | `go test ./internal/sandbox ./internal/session ./internal/store -count=1`; full `go test ./... -count=1` PASS |
| #64.2 | Duplicate-send test: two `Exec` calls with the same `tool_use_id` and a side-effect-counting tool → the tool executes **exactly once** and both calls return the same result. | pass | `internal/sandbox.(*Client).Exec`; `Reclaim`; `internal/session.RepairWithReclaim` | `TestDuplicateExecIsAbsorbedAndReclaimable`; `TestSessionRepairReclaimsCompletedResults`; `TestRepairWithReclaimPreservesOrderAndFabricatesMissing`; queue migration tests — inspected assertion: Duplicate-send test: two `Exec` calls with the same `tool_use_id` and a side-effect-counting tool → the tool executes **exactly once** and both calls return the same result. | n/a | `go test ./internal/sandbox ./internal/session ./internal/store -count=1`; full `go test ./... -count=1` PASS |
| #64.3 | Host-restart test: enqueue via one `Client`, drop it, let the runner complete, open a new `Client` and `Reclaim` → the completed result is adopted; the resumed transcript contains the tool's real output, not the fabricated "interrupted" error. | pass | `internal/sandbox.(*Client).Exec`; `Reclaim`; `internal/session.RepairWithReclaim` | `TestDuplicateExecIsAbsorbedAndReclaimable`; `TestSessionRepairReclaimsCompletedResults`; `TestRepairWithReclaimPreservesOrderAndFabricatesMissing`; queue migration tests — inspected assertion: Host-restart test: enqueue via one `Client`, drop it, let the runner complete, open a new `Client` and `Reclaim` → the completed result is adopted; the resumed transcript contains the tool's real output, not the fabricated "interrupted" error. | n/a | `go test ./internal/sandbox ./internal/session ./internal/store -count=1`; full `go test ./... -count=1` PASS |
| #64.4 | `Repair` fabricates an error result only for tool-use IDs with no reclaimable result; mixed case (one completed, one truly lost) covered by a test. | pass | `internal/sandbox.(*Client).Exec`; `Reclaim`; `internal/session.RepairWithReclaim` | `TestDuplicateExecIsAbsorbedAndReclaimable`; `TestSessionRepairReclaimsCompletedResults`; `TestRepairWithReclaimPreservesOrderAndFabricatesMissing`; queue migration tests — inspected assertion: `Repair` fabricates an error result only for tool-use IDs with no reclaimable result; mixed case (one completed, one truly lost) covered by a test. | n/a | `go test ./internal/sandbox ./internal/session ./internal/store -count=1`; full `go test ./... -count=1` PASS |
| #64.5 | Adopted results preserve the original `tool_use` block order in the closing user message. | pass | `internal/sandbox.(*Client).Exec`; `Reclaim`; `internal/session.RepairWithReclaim` | `TestDuplicateExecIsAbsorbedAndReclaimable`; `TestSessionRepairReclaimsCompletedResults`; `TestRepairWithReclaimPreservesOrderAndFabricatesMissing`; queue migration tests — inspected assertion: Adopted results preserve the original `tool_use` block order in the closing user message. | n/a | `go test ./internal/sandbox ./internal/session ./internal/store -count=1`; full `go test ./... -count=1` PASS |
| #64.6 | The retry design in #52 is constrained to reuse the original `tool_use_id` on re-delivery (absorption), never to mint a fresh identity for the same logical call — noted in that issue or its implementation docs. | pass | `internal/sandbox.(*Client).Exec`; `Reclaim`; `internal/session.RepairWithReclaim` | `TestDuplicateExecIsAbsorbedAndReclaimable`; `TestSessionRepairReclaimsCompletedResults`; `TestRepairWithReclaimPreservesOrderAndFabricatesMissing`; queue migration tests — inspected assertion: The retry design in #52 is constrained to reuse the original `tool_use_id` on re-delivery (absorption), never to mint a fresh identity for the same logical call — noted in that issue or its implementation docs. | artifact/source inspection required by criterion; no external mutation performed | `go test ./internal/sandbox ./internal/session ./internal/store -count=1`; full `go test ./... -count=1` PASS |
| #64.7 | `docs/plan.md` "Sandboxing & IPC" updated: the survive-restart property now genuinely holds on both sides of the queue pair. | pass | `internal/sandbox.(*Client).Exec`; `Reclaim`; `internal/session.RepairWithReclaim` | `TestDuplicateExecIsAbsorbedAndReclaimable`; `TestSessionRepairReclaimsCompletedResults`; `TestRepairWithReclaimPreservesOrderAndFabricatesMissing`; queue migration tests — inspected assertion: `docs/plan.md` "Sandboxing & IPC" updated: the survive-restart property now genuinely holds on both sides of the queue pair. | artifact/source inspection required by criterion; no external mutation performed | `go test ./internal/sandbox ./internal/session ./internal/store -count=1`; full `go test ./... -count=1` PASS |
| #65.1 | `waffle learn` mines sessions since the last run and prints recurring failure patterns with counts and evidence session IDs; a fixture DB with three seeded failure classes yields exactly the expected clusters (deterministic, test-covered). | pass | `internal/skill.Mine`; `Propose`; `ValidatePromotion`; `cmd/waffle.learnCmd` | `TestFullLearnRun`; skill mining/cache/promotion tests; `TestDistillRefusesActiveOverwrite` — inspected assertion: `waffle learn` mines sessions since the last run and prints recurring failure patterns with counts and evidence session IDs; a fixture DB with three seeded failure classes yields exactly the expected clusters (deterministic, test-covered). | n/a | `go test ./internal/skill ./internal/memory ./internal/eval ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #65.2 | Attribution calls use the utility model (#61) and are cached — re-running `learn` on unchanged data makes zero provider calls. | pass | `internal/skill.Mine`; `Propose`; `ValidatePromotion`; `cmd/waffle.learnCmd` | `TestFullLearnRun`; skill mining/cache/promotion tests; `TestDistillRefusesActiveOverwrite` — inspected assertion: Attribution calls use the utility model (#61) and are cached — re-running `learn` on unchanged data makes zero provider calls. | n/a | `go test ./internal/skill ./internal/memory ./internal/eval ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #65.3 | Proposals are constrained to enumerated surfaces; a proposal targeting anything else is rejected mechanically (test). | pass | `internal/skill.Mine`; `Propose`; `ValidatePromotion`; `cmd/waffle.learnCmd` | `TestFullLearnRun`; skill mining/cache/promotion tests; `TestDistillRefusesActiveOverwrite` — inspected assertion: Proposals are constrained to enumerated surfaces; a proposal targeting anything else is rejected mechanically (test). | n/a | `go test ./internal/skill ./internal/memory ./internal/eval ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #65.4 | The promotion rule is exactly the conservative criterion above; an edit improving held-in but regressing held-out is rejected (test with a rigged eval pair). | pass | `internal/skill.Mine`; `Propose`; `ValidatePromotion`; `cmd/waffle.learnCmd` | `TestFullLearnRun`; skill mining/cache/promotion tests; `TestDistillRefusesActiveOverwrite` — inspected assertion: The promotion rule is exactly the conservative criterion above; an edit improving held-in but regressing held-out is rejected (test with a rigged eval pair). | n/a | `go test ./internal/skill ./internal/memory ./internal/eval ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #65.5 | Every accepted edit lands as a git commit whose message links the pattern and the eval delta; every rejected proposal is stored with its audit record and shown to subsequent proposal rounds. | pass | `internal/skill.Mine`; `Propose`; `ValidatePromotion`; `cmd/waffle.learnCmd` | `TestFullLearnRun`; skill mining/cache/promotion tests; `TestDistillRefusesActiveOverwrite` — inspected assertion: Every accepted edit lands as a git commit whose message links the pattern and the eval delta; every rejected proposal is stored with its audit record and shown to subsequent proposal rounds. | n/a | `go test ./internal/skill ./internal/memory ./internal/eval ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #65.6 | `distill_skill` writes candidates as inactive until validated or owner-approved; overwriting an existing skill without validation is no longer possible; the skills index only lists active skills. | pass | `internal/skill.Mine`; `Propose`; `ValidatePromotion`; `cmd/waffle.learnCmd` | `TestFullLearnRun`; skill mining/cache/promotion tests; `TestDistillRefusesActiveOverwrite` — inspected assertion: `distill_skill` writes candidates as inactive until validated or owner-approved; overwriting an existing skill without validation is no longer possible; the skills index only lists active skills. | n/a | `go test ./internal/skill ./internal/memory ./internal/eval ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #65.7 | The learn pipeline can run as a scheduled job (`waffle cron`) and reports a digest to the configured channel. | pass | `internal/skill.Mine`; `Propose`; `ValidatePromotion`; `cmd/waffle.learnCmd` | `TestFullLearnRun`; skill mining/cache/promotion tests; `TestDistillRefusesActiveOverwrite` — inspected assertion: The learn pipeline can run as a scheduled job (`waffle cron`) and reports a digest to the configured channel. | n/a | `go test ./internal/skill ./internal/memory ./internal/eval ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #65.8 | `docs/plan.md` Phase 7 section updated to describe the mine→propose→validate loop. | pass | `internal/skill.Mine`; `Propose`; `ValidatePromotion`; `cmd/waffle.learnCmd` | `TestFullLearnRun`; skill mining/cache/promotion tests; `TestDistillRefusesActiveOverwrite` — inspected assertion: `docs/plan.md` Phase 7 section updated to describe the mine→propose→validate loop. | artifact/source inspection required by criterion; no external mutation performed | `go test ./internal/skill ./internal/memory ./internal/eval ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #66.1 | `[[policy.rule]]` parses with unknown-key rejection; rules are validated at load (unknown tool, empty match → config error). | pass | `internal/policy.(*Engine).CheckSession`; `ObserveSuccess`; `Narrow`; `internal/config.PolicyRuleConfig` | `TestTestsBeforeCommitRequireE2E`; `TestDenyWithGuidanceFeedback`; `TestChildPolicyCannotWidenParent`; `TestPolicyAuditRow`; config parse tests — inspected assertion: `[[policy.rule]]` parses with unknown-key rejection; rules are validated at load (unknown tool, empty match → config error). | n/a | `go test ./internal/policy ./internal/config ./internal/agent ./internal/repopolicy -count=1`; full `go test ./... -count=1` PASS |
| #66.2 | A `deny` rule blocks a matching `bash` call **before** dispatch (host-side, never reaching the sandbox queue) and the tool result contains the rule name and `because` text; non-matching calls are unaffected (table-driven tests). | pass | `internal/policy.(*Engine).CheckSession`; `ObserveSuccess`; `Narrow`; `internal/config.PolicyRuleConfig` | `TestTestsBeforeCommitRequireE2E`; `TestDenyWithGuidanceFeedback`; `TestChildPolicyCannotWidenParent`; `TestPolicyAuditRow`; config parse tests — inspected assertion: A `deny` rule blocks a matching `bash` call **before** dispatch (host-side, never reaching the sandbox queue) and the tool result contains the rule name and `because` text; non-matching calls are unaffected (table-driven tests). | n/a | `go test ./internal/policy ./internal/config ./internal/agent ./internal/repopolicy -count=1`; full `go test ./... -count=1` PASS |
| #66.3 | A `require` rule blocks until its predicate event has occurred in-session after the most recent matching write; the full tests-before-commit scenario is covered end-to-end (edit → commit blocked → test passes → commit allowed). | pass | `internal/policy.(*Engine).CheckSession`; `ObserveSuccess`; `Narrow`; `internal/config.PolicyRuleConfig` | `TestTestsBeforeCommitRequireE2E`; `TestDenyWithGuidanceFeedback`; `TestChildPolicyCannotWidenParent`; `TestPolicyAuditRow`; config parse tests — inspected assertion: A `require` rule blocks until its predicate event has occurred in-session after the most recent matching write; the full tests-before-commit scenario is covered end-to-end (edit → commit blocked → test passes → commit allowed). | n/a | `go test ./internal/policy ./internal/config ./internal/agent ./internal/repopolicy -count=1`; full `go test ./... -count=1` PASS |
| #66.4 | Denial messages are stable and documented — they are part of the model-facing interface. | pass | `internal/policy.(*Engine).CheckSession`; `ObserveSuccess`; `Narrow`; `internal/config.PolicyRuleConfig` | `TestTestsBeforeCommitRequireE2E`; `TestDenyWithGuidanceFeedback`; `TestChildPolicyCannotWidenParent`; `TestPolicyAuditRow`; config parse tests — inspected assertion: Denial messages are stable and documented — they are part of the model-facing interface. | artifact/source inspection required by criterion; no external mutation performed | `go test ./internal/policy ./internal/config ./internal/agent ./internal/repopolicy -count=1`; full `go test ./... -count=1` PASS |
| #66.5 | Subagent/workspace policies can only *narrow* the parent session's policy: a child policy attempting to allow something the parent denies is rejected at construction (hierarchical-domain rule; test). Applies to #33's per-agent-group policies when those land. | pass | `internal/policy.(*Engine).CheckSession`; `ObserveSuccess`; `Narrow`; `internal/config.PolicyRuleConfig` | `TestTestsBeforeCommitRequireE2E`; `TestDenyWithGuidanceFeedback`; `TestChildPolicyCannotWidenParent`; `TestPolicyAuditRow`; config parse tests — inspected assertion: Subagent/workspace policies can only *narrow* the parent session's policy: a child policy attempting to allow something the parent denies is rejected at construction (hierarchical-domain rule; test). Applies to #33's per-agent-group policies when those land. | n/a | `go test ./internal/policy ./internal/config ./internal/agent ./internal/repopolicy -count=1`; full `go test ./... -count=1` PASS |
| #66.6 | Matching is over parsed argv where the input is structured (bash command string split respecting quotes), with the known limitation (indirection) stated in the docs — no claim of OS-level guarantees. | pass | `internal/policy.(*Engine).CheckSession`; `ObserveSuccess`; `Narrow`; `internal/config.PolicyRuleConfig` | `TestTestsBeforeCommitRequireE2E`; `TestDenyWithGuidanceFeedback`; `TestChildPolicyCannotWidenParent`; `TestPolicyAuditRow`; config parse tests — inspected assertion: Matching is over parsed argv where the input is structured (bash command string split respecting quotes), with the known limitation (indirection) stated in the docs — no claim of OS-level guarantees. | artifact/source inspection required by criterion; no external mutation performed | `go test ./internal/policy ./internal/config ./internal/agent ./internal/repopolicy -count=1`; full `go test ./... -count=1` PASS |
| #66.7 | `[sandbox] enforcer` config key exists, defaults to `none`, and its docs explain the layered trust model (deterministic-with-feedback vs. hard container boundary). | pass | `internal/policy.(*Engine).CheckSession`; `ObserveSuccess`; `Narrow`; `internal/config.PolicyRuleConfig` | `TestTestsBeforeCommitRequireE2E`; `TestDenyWithGuidanceFeedback`; `TestChildPolicyCannotWidenParent`; `TestPolicyAuditRow`; config parse tests — inspected assertion: `[sandbox] enforcer` config key exists, defaults to `none`, and its docs explain the layered trust model (deterministic-with-feedback vs. hard container boundary). | artifact/source inspection required by criterion; no external mutation performed | `go test ./internal/policy ./internal/config ./internal/agent ./internal/repopolicy -count=1`; full `go test ./... -count=1` PASS |
| #66.8 | Policy decisions (rule, session, verdict) are logged like broker grants (audit parity with `internal/secret`'s audit rows). | pass | `internal/policy.(*Engine).CheckSession`; `ObserveSuccess`; `Narrow`; `internal/config.PolicyRuleConfig` | `TestTestsBeforeCommitRequireE2E`; `TestDenyWithGuidanceFeedback`; `TestChildPolicyCannotWidenParent`; `TestPolicyAuditRow`; config parse tests — inspected assertion: Policy decisions (rule, session, verdict) are logged like broker grants (audit parity with `internal/secret`'s audit rows). | n/a | `go test ./internal/policy ./internal/config ./internal/agent ./internal/repopolicy -count=1`; full `go test ./... -count=1` PASS |
| #67.1 | `WorkingSet` supports `add`, `replace`, `drop` and `clear` through owner/application APIs. | pass | `internal/workset.Store`; `UpdateTool`; `Render`; `internal/eval.Registry` workset cases | `TestAddCapAndRender`; `TestReplaceDropClear`; `TestUpdateToolAndDropAssumptions`; evals `workset-retention`, `workset-reversal`, `workset-false-assumption`, `workset-hostile-output` — inspected assertion: `WorkingSet` supports `add`, `replace`, `drop` and `clear` through owner/application APIs. | n/a | `go test ./internal/workset ./internal/session ./internal/eval ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
| #67.2 | Entries include stable id, kind, source and text; invalid kind/source values are rejected. | pass | `internal/workset.Store`; `UpdateTool`; `Render`; `internal/eval.Registry` workset cases | `TestAddCapAndRender`; `TestReplaceDropClear`; `TestUpdateToolAndDropAssumptions`; evals `workset-retention`, `workset-reversal`, `workset-false-assumption`, `workset-hostile-output` — inspected assertion: Entries include stable id, kind, source and text; invalid kind/source values are rejected. | n/a | `go test ./internal/workset ./internal/session ./internal/eval ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
| #67.3 | Hard caps on entry count, per-entry byte length and total rendered bytes are enforced with documented defaults. | pass | `internal/workset.Store`; `UpdateTool`; `Render`; `internal/eval.Registry` workset cases | `TestAddCapAndRender`; `TestReplaceDropClear`; `TestUpdateToolAndDropAssumptions`; evals `workset-retention`, `workset-reversal`, `workset-false-assumption`, `workset-hostile-output` — inspected assertion: Hard caps on entry count, per-entry byte length and total rendered bytes are enforced with documented defaults. | artifact/source inspection required by criterion; no external mutation performed | `go test ./internal/workset ./internal/session ./internal/eval ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
| #67.4 | An over-cap `add` fails and identifies the entries that must be dropped or replaced; no silent eviction. | pass | `internal/workset.Store`; `UpdateTool`; `Render`; `internal/eval.Registry` workset cases | `TestAddCapAndRender`; `TestReplaceDropClear`; `TestUpdateToolAndDropAssumptions`; evals `workset-retention`, `workset-reversal`, `workset-false-assumption`, `workset-hostile-output` — inspected assertion: An over-cap `add` fails and identifies the entries that must be dropped or replaced; no silent eviction. | n/a | `go test ./internal/workset ./internal/session ./internal/eval ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
| #67.5 | The rendered set is supplied on every provider call in a dedicated, mixed-provenance section before any context summary. | pass | `internal/workset.Store`; `UpdateTool`; `Render`; `internal/eval.Registry` workset cases | `TestAddCapAndRender`; `TestReplaceDropClear`; `TestUpdateToolAndDropAssumptions`; evals `workset-retention`, `workset-reversal`, `workset-false-assumption`, `workset-hostile-output` — inspected assertion: The rendered set is supplied on every provider call in a dedicated, mixed-provenance section before any context summary. | n/a | `go test ./internal/workset ./internal/session ./internal/eval ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
| #67.6 | `prepareContext` and `summarize` never receive, truncate or rewrite working-set content. | pass | `internal/workset.Store`; `UpdateTool`; `Render`; `internal/eval.Registry` workset cases | `TestAddCapAndRender`; `TestReplaceDropClear`; `TestUpdateToolAndDropAssumptions`; evals `workset-retention`, `workset-reversal`, `workset-false-assumption`, `workset-hostile-output` — inspected assertion: `prepareContext` and `summarize` never receive, truncate or rewrite working-set content. | n/a | `go test ./internal/workset ./internal/session ./internal/eval ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
| #67.7 | The set persists per session and survives store close/reopen and `chat -c`. | pass | `internal/workset.Store`; `UpdateTool`; `Render`; `internal/eval.Registry` workset cases | `TestAddCapAndRender`; `TestReplaceDropClear`; `TestUpdateToolAndDropAssumptions`; evals `workset-retention`, `workset-reversal`, `workset-false-assumption`, `workset-hostile-output` — inspected assertion: The set persists per session and survives store close/reopen and `chat -c`. | n/a | `go test ./internal/workset ./internal/session ./internal/eval ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
| #67.8 | `/reset`, session deletion and workspace-session switching have explicit tested lifecycle behaviour. | pass | `internal/workset.Store`; `UpdateTool`; `Render`; `internal/eval.Registry` workset cases | `TestAddCapAndRender`; `TestReplaceDropClear`; `TestUpdateToolAndDropAssumptions`; evals `workset-retention`, `workset-reversal`, `workset-false-assumption`, `workset-hostile-output` — inspected assertion: `/reset`, session deletion and workspace-session switching have explicit tested lifecycle behaviour. | n/a | `go test ./internal/workset ./internal/session ./internal/eval ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
| #67.9 | CLI/status inspection and correction are available. | pass | `internal/workset.Store`; `UpdateTool`; `Render`; `internal/eval.Registry` workset cases | `TestAddCapAndRender`; `TestReplaceDropClear`; `TestUpdateToolAndDropAssumptions`; evals `workset-retention`, `workset-reversal`, `workset-false-assumption`, `workset-hostile-output` — inspected assertion: CLI/status inspection and correction are available. | n/a | `go test ./internal/workset ./internal/session ./internal/eval ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
| #67.10 | Unit tests cover cap boundary, replace/drop/clear, multibyte byte limits, empty rendering, provenance rendering and persistence. | pass | `internal/workset.Store`; `UpdateTool`; `Render`; `internal/eval.Registry` workset cases | `TestAddCapAndRender`; `TestReplaceDropClear`; `TestUpdateToolAndDropAssumptions`; evals `workset-retention`, `workset-reversal`, `workset-false-assumption`, `workset-hostile-output` — inspected assertion: Unit tests cover cap boundary, replace/drop/clear, multibyte byte limits, empty rendering, provenance rendering and persistence. | n/a | `go test ./internal/workset ./internal/session ./internal/eval ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
| #67.11 | Evaluation cases cover retention, reversal, stale assumptions and prompt injection against a no-working-set baseline. | pass | `internal/workset.Store`; `UpdateTool`; `Render`; `internal/eval.Registry` workset cases | `TestAddCapAndRender`; `TestReplaceDropClear`; `TestUpdateToolAndDropAssumptions`; evals `workset-retention`, `workset-reversal`, `workset-false-assumption`, `workset-hostile-output` — inspected assertion: Evaluation cases cover retention, reversal, stale assumptions and prompt injection against a no-working-set baseline. | n/a | `go test ./internal/workset ./internal/session ./internal/eval ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
| #67.12 | Model mutation is exposed as proposals or a `workspace_update` tool according to agent-group policy. | pass | `internal/workset.Store`; `UpdateTool`; `Render`; `internal/eval.Registry` workset cases | `TestAddCapAndRender`; `TestReplaceDropClear`; `TestUpdateToolAndDropAssumptions`; evals `workset-retention`, `workset-reversal`, `workset-false-assumption`, `workset-hostile-output` — inspected assertion: Model mutation is exposed as proposals or a `workspace_update` tool according to agent-group policy. | n/a | `go test ./internal/workset ./internal/session ./internal/eval ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
| #67.13 | Every model mutation is marked `source=model`, visible to the owner and audit-recorded. | pass | `internal/workset.Store`; `UpdateTool`; `Render`; `internal/eval.Registry` workset cases | `TestAddCapAndRender`; `TestReplaceDropClear`; `TestUpdateToolAndDropAssumptions`; evals `workset-retention`, `workset-reversal`, `workset-false-assumption`, `workset-hostile-output` — inspected assertion: Every model mutation is marked `source=model`, visible to the owner and audit-recorded. | n/a | `go test ./internal/workset ./internal/session ./internal/eval ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
| #67.14 | Unattended groups have a documented conservative default for working-set writes. | pass | `internal/workset.Store`; `UpdateTool`; `Render`; `internal/eval.Registry` workset cases | `TestAddCapAndRender`; `TestReplaceDropClear`; `TestUpdateToolAndDropAssumptions`; evals `workset-retention`, `workset-reversal`, `workset-false-assumption`, `workset-hostile-output` — inspected assertion: Unattended groups have a documented conservative default for working-set writes. | artifact/source inspection required by criterion; no external mutation performed | `go test ./internal/workset ./internal/session ./internal/eval ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
| #67.15 | Subagents do not receive mutation authority by default; any read-only inheritance is a separate deliberate decision. | pass | `internal/workset.Store`; `UpdateTool`; `Render`; `internal/eval.Registry` workset cases | `TestAddCapAndRender`; `TestReplaceDropClear`; `TestUpdateToolAndDropAssumptions`; evals `workset-retention`, `workset-reversal`, `workset-false-assumption`, `workset-hostile-output` — inspected assertion: Subagents do not receive mutation authority by default; any read-only inheritance is a separate deliberate decision. | n/a | `go test ./internal/workset ./internal/session ./internal/eval ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
| #67.16 | A model cannot relabel its own content as `user` or `system`. | pass | `internal/workset.Store`; `UpdateTool`; `Render`; `internal/eval.Registry` workset cases | `TestAddCapAndRender`; `TestReplaceDropClear`; `TestUpdateToolAndDropAssumptions`; evals `workset-retention`, `workset-reversal`, `workset-false-assumption`, `workset-hostile-output` — inspected assertion: A model cannot relabel its own content as `user` or `system`. | n/a | `go test ./internal/workset ./internal/session ./internal/eval ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
| #67.17 | Tests cover hostile tool output, denied mutation policy and correction of a model-authored false assumption. | pass | `internal/workset.Store`; `UpdateTool`; `Render`; `internal/eval.Registry` workset cases | `TestAddCapAndRender`; `TestReplaceDropClear`; `TestUpdateToolAndDropAssumptions`; evals `workset-retention`, `workset-reversal`, `workset-false-assumption`, `workset-hostile-output` — inspected assertion: Tests cover hostile tool output, denied mutation policy and correction of a model-authored false assumption. | n/a | `go test ./internal/workset ./internal/session ./internal/eval ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
| #68.1 | With a non-empty parent working set and the config default, the subagent's system prompt contains the rendered set inside the framed section; with an empty set or `broadcast_working_set = false`, the system prompt is byte-identical to today's (tests for all three). | pass | `internal/agent.SubagentTool.Run`; `NormalizeHandoff`; `internal/workset.ValidateProposal` | `TestParallelSubagentsFrozenSnapshotDespiteParentMutation`; `TestParentWorksetUnchangedAfterSubagentProposals`; `TestEmptyBroadcastSystemByteIdentical` — inspected assertion: With a non-empty parent working set and the config default, the subagent's system prompt contains the rendered set inside the framed section; with an empty set or `broadcast_working_set = false`, the system prompt is byte-identical to today's (tests for all three). | n/a | `go test -race ./internal/agent ./internal/workset ./internal/session -count=1`; full `go test ./... -count=1` PASS |
| #68.2 | The broadcast is read-only by construction: subagent toolboxes never contain `workspace_update` (test), and no code path mutates the parent's set during a subagent run (test asserts parent set deep-equal before/after a run whose subagent emits proposals). | pass | `internal/agent.SubagentTool.Run`; `NormalizeHandoff`; `internal/workset.ValidateProposal` | `TestParallelSubagentsFrozenSnapshotDespiteParentMutation`; `TestParentWorksetUnchangedAfterSubagentProposals`; `TestEmptyBroadcastSystemByteIdentical` — inspected assertion: The broadcast is read-only by construction: subagent toolboxes never contain `workspace_update` (test), and no code path mutates the parent's set during a subagent run (test asserts parent set deep-equal before/after a run whose subagent emits proposals). | n/a | `go test -race ./internal/agent ./internal/workset ./internal/session -count=1`; full `go test ./... -count=1` PASS |
| #68.3 | Proposal parsing is robust: absent block → tool result unchanged from today's shape; malformed block → treated as prose, not an error; multiple entries parsed in order; each proposal is validated against #67's per-entry caps and `Kind` enum before being surfaced, and oversized/unknown-kind proposals are surfaced as rejected with the reason. | pass | `internal/agent.SubagentTool.Run`; `NormalizeHandoff`; `internal/workset.ValidateProposal` | `TestParallelSubagentsFrozenSnapshotDespiteParentMutation`; `TestParentWorksetUnchangedAfterSubagentProposals`; `TestEmptyBroadcastSystemByteIdentical` — inspected assertion: Proposal parsing is robust: absent block → tool result unchanged from today's shape; malformed block → treated as prose, not an error; multiple entries parsed in order; each proposal is validated against #67's per-entry caps and `Kind` enum before being surfaced, and oversized/unknown-kind proposals are surfaced as rejected with the reason. | n/a | `go test -race ./internal/agent ./internal/workset ./internal/session -count=1`; full `go test ./... -count=1` PASS |
| #68.4 | Proposals appear in the tool result only under the explicit not-applied marker; grep-level test that the string cannot be confused with an applied confirmation. | pass | `internal/agent.SubagentTool.Run`; `NormalizeHandoff`; `internal/workset.ValidateProposal` | `TestParallelSubagentsFrozenSnapshotDespiteParentMutation`; `TestParentWorksetUnchangedAfterSubagentProposals`; `TestEmptyBroadcastSystemByteIdentical` — inspected assertion: Proposals appear in the tool result only under the explicit not-applied marker; grep-level test that the string cannot be confused with an applied confirmation. | n/a | `go test -race ./internal/agent ./internal/workset ./internal/session -count=1`; full `go test ./... -count=1` PASS |
| #68.5 | Parallel spawn test: three subagents in one turn all receive the same broadcast snapshot (the set as of dispatch), even if the parent's set changes between result arrivals. | pass | `internal/agent.SubagentTool.Run`; `NormalizeHandoff`; `internal/workset.ValidateProposal` | `TestParallelSubagentsFrozenSnapshotDespiteParentMutation`; `TestParentWorksetUnchangedAfterSubagentProposals`; `TestEmptyBroadcastSystemByteIdentical` — inspected assertion: Parallel spawn test: three subagents in one turn all receive the same broadcast snapshot (the set as of dispatch), even if the parent's set changes between result arrivals. | n/a | `go test -race ./internal/agent ./internal/workset ./internal/session -count=1`; full `go test ./... -count=1` PASS |
| #68.6 | Depth interaction documented: sub-toolboxes omit `spawn_subagent` today (`subagent.go:28-31`), so nested broadcast is out of scope; a comment says what should happen if that changes (child inherits the same read-only broadcast, never a widened one). | pass | `internal/agent.SubagentTool.Run`; `NormalizeHandoff`; `internal/workset.ValidateProposal` | `TestParallelSubagentsFrozenSnapshotDespiteParentMutation`; `TestParentWorksetUnchangedAfterSubagentProposals`; `TestEmptyBroadcastSystemByteIdentical` — inspected assertion: Depth interaction documented: sub-toolboxes omit `spawn_subagent` today (`subagent.go:28-31`), so nested broadcast is out of scope; a comment says what should happen if that changes (child inherits the same read-only broadcast, never a widened one). | artifact/source inspection required by criterion; no external mutation performed | `go test -race ./internal/agent ./internal/workset ./internal/session -count=1`; full `go test ./... -count=1` PASS |
| #68.7 | `docs/plan.md` subagent section updated with the broadcast/propose protocol and its trust rationale. | pass | `internal/agent.SubagentTool.Run`; `NormalizeHandoff`; `internal/workset.ValidateProposal` | `TestParallelSubagentsFrozenSnapshotDespiteParentMutation`; `TestParentWorksetUnchangedAfterSubagentProposals`; `TestEmptyBroadcastSystemByteIdentical` — inspected assertion: `docs/plan.md` subagent section updated with the broadcast/propose protocol and its trust rationale. | artifact/source inspection required by criterion; no external mutation performed | `go test -race ./internal/agent ./internal/workset ./internal/session -count=1`; full `go test ./... -count=1` PASS |
| #69.1 | Any host-executed tool output over `OutputLimit` is spilled in full (up to the spill cap) before truncation; the transcript result carries the handle-bearing marker. Output at or under the limit spills nothing and is byte-identical to today's behaviour (test at the boundary). | pass | `internal/spill.Store`; `ExpandTool.Run`; `internal/agent.(*Agent).runOne`; `internal/memory.RecallTool.Run` | `TestSpillBoundaryAndExpand`; `TestSpillCapPartialMarker`; `TestRedactBeforeSpillSecretNeverOnDisk`; `TestRecallSpillsScope`; session spill-delete tests — inspected assertion: Any host-executed tool output over `OutputLimit` is spilled in full (up to the spill cap) before truncation; the transcript result carries the handle-bearing marker. Output at or under the limit spills nothing and is byte-identical to today's behaviour (test at the boundary). | n/a | `go test ./internal/spill ./internal/agent ./internal/memory ./internal/session ./internal/tool -count=1`; full `go test ./... -count=1` PASS |
| #69.2 | `expand_output` returns exact byte ranges and grep matches with surrounding context from spilled content; every response is itself bounded by `Truncate` (test with a spilled output several times `OutputLimit`). | pass | `internal/spill.Store`; `ExpandTool.Run`; `internal/agent.(*Agent).runOne`; `internal/memory.RecallTool.Run` | `TestSpillBoundaryAndExpand`; `TestSpillCapPartialMarker`; `TestRedactBeforeSpillSecretNeverOnDisk`; `TestRecallSpillsScope`; session spill-delete tests — inspected assertion: `expand_output` returns exact byte ranges and grep matches with surrounding context from spilled content; every response is itself bounded by `Truncate` (test with a spilled output several times `OutputLimit`). | n/a | `go test ./internal/spill ./internal/agent ./internal/memory ./internal/session ./internal/tool -count=1`; full `go test ./... -count=1` PASS |
| #69.3 | An `expand_output` call with an unknown id, an out-of-range offset, or a pattern with no matches returns a descriptive non-crashing result (tests). | pass | `internal/spill.Store`; `ExpandTool.Run`; `internal/agent.(*Agent).runOne`; `internal/memory.RecallTool.Run` | `TestSpillBoundaryAndExpand`; `TestSpillCapPartialMarker`; `TestRedactBeforeSpillSecretNeverOnDisk`; `TestRecallSpillsScope`; session spill-delete tests — inspected assertion: An `expand_output` call with an unknown id, an out-of-range offset, or a pattern with no matches returns a descriptive non-crashing result (tests). | n/a | `go test ./internal/spill ./internal/agent ./internal/memory ./internal/session ./internal/tool -count=1`; full `go test ./... -count=1` PASS |
| #69.4 | The spill cap is enforced: output beyond it is spilled up to the cap and the marker says the spill itself is partial (test). | pass | `internal/spill.Store`; `ExpandTool.Run`; `internal/agent.(*Agent).runOne`; `internal/memory.RecallTool.Run` | `TestSpillBoundaryAndExpand`; `TestSpillCapPartialMarker`; `TestRedactBeforeSpillSecretNeverOnDisk`; `TestRecallSpillsScope`; session spill-delete tests — inspected assertion: The spill cap is enforced: output beyond it is spilled up to the cap and the marker says the spill itself is partial (test). | n/a | `go test ./internal/spill ./internal/agent ./internal/memory ./internal/session ./internal/tool -count=1`; full `go test ./... -count=1` PASS |
| #69.5 | Spilled content is FTS-searchable and `recall` hits render with a distinct source label; a test proves a string that exists only in the truncated middle of a large output is findable after this change and was not before. | pass | `internal/spill.Store`; `ExpandTool.Run`; `internal/agent.(*Agent).runOne`; `internal/memory.RecallTool.Run` | `TestSpillBoundaryAndExpand`; `TestSpillCapPartialMarker`; `TestRedactBeforeSpillSecretNeverOnDisk`; `TestRecallSpillsScope`; session spill-delete tests — inspected assertion: Spilled content is FTS-searchable and `recall` hits render with a distinct source label; a test proves a string that exists only in the truncated middle of a large output is findable after this change and was not before. | n/a | `go test ./internal/spill ./internal/agent ./internal/memory ./internal/session ./internal/tool -count=1`; full `go test ./... -count=1` PASS |
| #69.6 | Spill rows carry `session_id` and `created_at` so #47's retention/forget work can reach them; deleting a session's spills is a single indexed statement (test). | pass | `internal/spill.Store`; `ExpandTool.Run`; `internal/agent.(*Agent).runOne`; `internal/memory.RecallTool.Run` | `TestSpillBoundaryAndExpand`; `TestSpillCapPartialMarker`; `TestRedactBeforeSpillSecretNeverOnDisk`; `TestRecallSpillsScope`; session spill-delete tests — inspected assertion: Spill rows carry `session_id` and `created_at` so #47's retention/forget work can reach them; deleting a session's spills is a single indexed statement (test). | n/a | `go test ./internal/spill ./internal/agent ./internal/memory ./internal/session ./internal/tool -count=1`; full `go test ./... -count=1` PASS |
| #69.7 | Redaction ordering is preserved: `Agent.Redact` currently scrubs the truncated result (`internal/agent/agent.go:186-188`) — spilled content must pass through the same redactor before storage, and a test proves a secret in the truncated middle never reaches disk unredacted. | pass | `internal/spill.Store`; `ExpandTool.Run`; `internal/agent.(*Agent).runOne`; `internal/memory.RecallTool.Run` | `TestSpillBoundaryAndExpand`; `TestSpillCapPartialMarker`; `TestRedactBeforeSpillSecretNeverOnDisk`; `TestRecallSpillsScope`; session spill-delete tests — inspected assertion: Redaction ordering is preserved: `Agent.Redact` currently scrubs the truncated result (`internal/agent/agent.go:186-188`) — spilled content must pass through the same redactor before storage, and a test proves a secret in the truncated middle never reaches disk unredacted. | n/a | `go test ./internal/spill ./internal/agent ./internal/memory ./internal/session ./internal/tool -count=1`; full `go test ./... -count=1` PASS |
| #69.8 | Sandbox mode: behaviour unchanged, limitation documented in code comment and `docs/plan.md`. | pass | `internal/spill.Store`; `ExpandTool.Run`; `internal/agent.(*Agent).runOne`; `internal/memory.RecallTool.Run` | `TestSpillBoundaryAndExpand`; `TestSpillCapPartialMarker`; `TestRedactBeforeSpillSecretNeverOnDisk`; `TestRecallSpillsScope`; session spill-delete tests — inspected assertion: Sandbox mode: behaviour unchanged, limitation documented in code comment and `docs/plan.md`. | artifact/source inspection required by criterion; no external mutation performed | `go test ./internal/spill ./internal/agent ./internal/memory ./internal/session ./internal/tool -count=1`; full `go test ./... -count=1` PASS |
| #69.9 | `docs/plan.md` tool-layer section describes spill/expand and its budget rationale. | pass | `internal/spill.Store`; `ExpandTool.Run`; `internal/agent.(*Agent).runOne`; `internal/memory.RecallTool.Run` | `TestSpillBoundaryAndExpand`; `TestSpillCapPartialMarker`; `TestRedactBeforeSpillSecretNeverOnDisk`; `TestRecallSpillsScope`; session spill-delete tests — inspected assertion: `docs/plan.md` tool-layer section describes spill/expand and its budget rationale. | artifact/source inspection required by criterion; no external mutation performed | `go test ./internal/spill ./internal/agent ./internal/memory ./internal/session ./internal/tool -count=1`; full `go test ./... -count=1` PASS |
| #70.1 | Working-set maintenance never invents new information; it only drop/replaces existing entries. | pass | `internal/workset.Store.DropStaleAssumptionsAll`; maintenance wiring in `cmd/waffle` | workset maintenance replacement/drop/convergence tests; `TestUpdateToolAndDropAssumptions` — inspected assertion: Working-set maintenance never invents new information; it only drop/replaces existing entries. | n/a | `go test ./internal/workset ./internal/session ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #70.2 | Contradictory entries are resolved by replacement rather than accumulation. | pass | `internal/workset.Store.DropStaleAssumptionsAll`; maintenance wiring in `cmd/waffle` | workset maintenance replacement/drop/convergence tests; `TestUpdateToolAndDropAssumptions` — inspected assertion: Contradictory entries are resolved by replacement rather than accumulation. | n/a | `go test ./internal/workset ./internal/session ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #70.3 | Completed goals no longer consume workspace capacity. | pass | `internal/workset.Store.DropStaleAssumptionsAll`; maintenance wiring in `cmd/waffle` | workset maintenance replacement/drop/convergence tests; `TestUpdateToolAndDropAssumptions` — inspected assertion: Completed goals no longer consume workspace capacity. | n/a | `go test ./internal/workset ./internal/session ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #70.4 | Long-running sessions converge on a stable working set instead of steadily filling to the cap. | pass | `internal/workset.Store.DropStaleAssumptionsAll`; maintenance wiring in `cmd/waffle` | workset maintenance replacement/drop/convergence tests; `TestUpdateToolAndDropAssumptions` — inspected assertion: Long-running sessions converge on a stable working set instead of steadily filling to the cap. | n/a | `go test ./internal/workset ./internal/session ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #70.5 | Documentation explains the three responsibilities: transcript (history), working set (active task state), MEMORY.md (durable knowledge). | pass | `internal/workset.Store.DropStaleAssumptionsAll`; maintenance wiring in `cmd/waffle` | workset maintenance replacement/drop/convergence tests; `TestUpdateToolAndDropAssumptions` — inspected assertion: Documentation explains the three responsibilities: transcript (history), working set (active task state), MEMORY.md (durable knowledge). | n/a | `go test ./internal/workset ./internal/session ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.1 | A default configuration with no profile section produces exactly one `main` profile whose effective behaviour is byte-for-byte compatible with today's default agent construction, except for added audit/status metadata. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: A default configuration with no profile section produces exactly one `main` profile whose effective behaviour is byte-for-byte compatible with today's default agent construction, except for added audit/status metadata. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.2 | A config with two valid profiles loads successfully and exposes both through a tested profile registry. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: A config with two valid profiles loads successfully and exposes both through a tested profile registry. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.3 | Duplicate profile names are rejected at config load with an error naming the duplicate. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: Duplicate profile names are rejected at config load with an error naming the duplicate. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.4 | Invalid profile names are rejected: empty string, whitespace, path separators, shell metacharacters, uppercase surprises if slug-only is chosen, and names longer than the documented max. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: Invalid profile names are rejected: empty string, whitespace, path separators, shell metacharacters, uppercase surprises if slug-only is chosen, and names longer than the documented max. | artifact/source inspection required by criterion; no external mutation performed | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.5 | Unknown config keys inside a profile are rejected, matching the existing config strictness. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: Unknown config keys inside a profile are rejected, matching the existing config strictness. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.6 | Unknown tool names in profile allow/deny are rejected with the profile name and bad tool name in the error. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: Unknown tool names in profile allow/deny are rejected with the profile name and bad tool name in the error. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.7 | Deny overrides allow in tests, including `allow = ["*"]` plus one denied tool. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: Deny overrides allow in tests, including `allow = ["*"]` plus one denied tool. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.8 | Missing prompt files, prompt paths outside allowed roots, and unreadable prompt files are config errors. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: Missing prompt files, prompt paths outside allowed roots, and unreadable prompt files are config errors. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.9 | Inline prompt and file prompt are both covered by tests. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: Inline prompt and file prompt are both covered by tests. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.10 | Empty prompt is allowed only via an explicit `system = ""` or equivalent documented form; accidental empty file/missing file is tested separately. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: Empty prompt is allowed only via an explicit `system = ""` or equivalent documented form; accidental empty file/missing file is tested separately. | artifact/source inspection required by criterion; no external mutation performed | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.11 | Building an agent from `main` with no config produces the same provider, model, system prompt, toolbox, max tokens, redactor and iteration behaviour as today. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: Building an agent from `main` with no config produces the same provider, model, system prompt, toolbox, max tokens, redactor and iteration behaviour as today. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.12 | Building an agent from a specialist profile uses that profile's system prompt, model selection, toolbox restriction and sandbox posture. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: Building an agent from a specialist profile uses that profile's system prompt, model selection, toolbox restriction and sandbox posture. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.13 | The constructed toolbox for a read-only profile does not include mutation tools in `Defs()` and direct calls to excluded tools fail before dispatch. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: The constructed toolbox for a read-only profile does not include mutation tools in `Defs()` and direct calls to excluded tools fail before dispatch. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.14 | Profile-specific model selection is tested for `default`, `utility`, and invalid/missing utility model cases. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: Profile-specific model selection is tested for `default`, `utility`, and invalid/missing utility model cases. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.15 | Profile-specific max-iteration / max-token settings are either supported and tested, or explicitly rejected as out of scope with config errors. No silently ignored knobs. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: Profile-specific max-iteration / max-token settings are either supported and tested, or explicitly rejected as out of scope with config errors. No silently ignored knobs. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.16 | `spawn_subagent` accepts an optional profile field and defaults in a backwards-compatible way when omitted. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: `spawn_subagent` accepts an optional profile field and defaults in a backwards-compatible way when omitted. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.17 | A parent can only delegate to profiles listed in its allowed child-profile set; disallowed profile names are rejected before constructing the child agent. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: A parent can only delegate to profiles listed in its allowed child-profile set; disallowed profile names are rejected before constructing the child agent. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.18 | A child profile cannot widen effective tool access beyond the parent/delegation policy. This must be enforced mechanically and covered by tests. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: A child profile cannot widen effective tool access beyond the parent/delegation policy. This must be enforced mechanically and covered by tests. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.19 | A read-only parent attempting to spawn a write-capable child is rejected even if the child profile exists. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: A read-only parent attempting to spawn a write-capable child is rejected even if the child profile exists. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.20 | A child receives its own profile system prompt; a test proves it does not receive the parent's system prompt except for explicitly documented read-only working-set broadcast from #68. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: A child receives its own profile system prompt; a test proves it does not receive the parent's system prompt except for explicitly documented read-only working-set broadcast from #68. | artifact/source inspection required by criterion; no external mutation performed | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.21 | Subagent depth/concurrency limits remain enforced after profile targeting. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: Subagent depth/concurrency limits remain enforced after profile targeting. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.22 | Subagent results/logs include the child profile name. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: Subagent results/logs include the child profile name. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.23 | Existing channel groups continue to run under `main` unless explicitly rebound. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: Existing channel groups continue to run under `main` unless explicitly rebound. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.24 | New channel groups default to `main`, with this default represented through the profile registry rather than a magic hardcoded string spread across the codebase. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: New channel groups default to `main`, with this default represented through the profile registry rather than a magic hardcoded string spread across the codebase. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.25 | There is a CLI/admin path to bind a channel group or conversation to a profile, and invalid profile names fail clearly. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: There is a CLI/admin path to bind a channel group or conversation to a profile, and invalid profile names fail clearly. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.26 | Cron jobs can specify a profile; omitted profile preserves current behaviour. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: Cron jobs can specify a profile; omitted profile preserves current behaviour. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.27 | Repo workspace sessions can be associated with a profile without allowing repo-owned config to escalate privileges. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: Repo workspace sessions can be associated with a profile without allowing repo-owned config to escalate privileges. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.28 | All three surfaces have tests proving the selected profile affects the actual system prompt and toolbox used by the run. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: All three surfaces have tests proving the selected profile affects the actual system prompt and toolbox used by the run. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.29 | Profile name is stored with session/run metadata, or otherwise recoverable for every run in a durable/auditable way. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: Profile name is stored with session/run metadata, or otherwise recoverable for every run in a durable/auditable way. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.30 | Logs include profile name for provider calls, tool calls, denials, subagent spawn, cron run start/end and gateway dispatch. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: Logs include profile name for provider calls, tool calls, denials, subagent spawn, cron run start/end and gateway dispatch. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.31 | If tool-policy denials happen under a profile, the denial includes the profile name and effective rule/policy source. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: If tool-policy denials happen under a profile, the denial includes the profile name and effective rule/policy source. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.32 | A profile change for a channel group is auditable: old profile, new profile, actor/source, timestamp. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: A profile change for a channel group is auditable: old profile, new profile, actor/source, timestamp. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.33 | Repo-provided policy files (#53) can only narrow the selected profile. A test covers a malicious repo policy trying to enable `bash` or host mode under a read-only profile. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: Repo-provided policy files (#53) can only narrow the selected profile. A test covers a malicious repo policy trying to enable `bash` or host mode under a read-only profile. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.34 | Documentation states that profiles are trust-boundary configuration, not personality presets only. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: Documentation states that profiles are trust-boundary configuration, not personality presets only. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.35 | `docs/plan.md` is updated to describe named profiles as the unit that combines system prompt, model, toolbox, sandbox posture and delegation policy. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: `docs/plan.md` is updated to describe named profiles as the unit that combines system prompt, model, toolbox, sandbox posture and delegation policy. | artifact/source inspection required by criterion; no external mutation performed | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.36 | Example config includes at least `main`, `researcher`, and `reviewer` profiles. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: Example config includes at least `main`, `researcher`, and `reviewer` profiles. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.37 | Docs explain the relationship between profiles, #33 agent-group trust tiering, #53 repo policy files, #66 action-level policy, and #68 subagent working-set broadcast. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: Docs explain the relationship between profiles, #33 agent-group trust tiering, #53 repo policy files, #66 action-level policy, and #68 subagent working-set broadcast. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #71.38 | Docs include a migration note: existing users get `main`; no config changes required. | pass | `internal/config.ProfileRegistry`; `cmd/waffle.buildAgent`; `internal/agent.ChildProfile`; profile bindings | `TestProfilesLoadRegistryAndDenyOverridesAllowStar`; `TestBuildAgentWithProfileSpecialist`; `TestGatewayUsesProfileAgent`; `TestRunnerUsesProfileAgent`; workspace profile tests — inspected assertion: Docs include a migration note: existing users get `main`; no config changes required. | n/a | `go test ./internal/config ./internal/agent ./internal/gateway ./internal/schedule ./internal/workspace ./cmd/waffle -count=1`; full `go test ./... -count=1` PASS |
| #72.1 | Meta/dependency-ordering issue; no independent acceptance criterion. | not-applicable | `docs/plan.md` dependency sequence | source snapshot has no executable criterion — inspected assertion: Meta/dependency-ordering issue; no independent acceptance criterion. | source issue has no checkbox; ordering inspected manually | `go test ./... -count=1`; full `go test ./... -count=1` PASS |
| #77.1 | MCP servers are filtered by group/tool policy **before** processes are launched. | pass | `cmd/waffle.buildAgent` applies `mcpServerInGroup` and `mcpServerPermitted` before `mcp.ConnectRestricted` | `TestDeniedMCPServerNotLaunched` asserts group/policy prefiltering before any connector call | n/a | `go test ./cmd/waffle ./internal/mcp ./internal/config ./internal/sandbox ./internal/selfdev -count=1`; focused tests above; full `go test ./... -count=1` PASS |
| #77.2 | A server with no callable tools in the resolved group is not started. | pass | `cmd/waffle.mcpServerPermitted`; launch loop `continue` before process construction | `TestDeniedMCPServerNotLaunched` asserts all-declared-tools-denied returns false | n/a | `go test ./cmd/waffle ./internal/mcp ./internal/config ./internal/sandbox ./internal/selfdev -count=1`; focused tests above; full `go test ./... -count=1` PASS |
| #77.3 | Docker/sandboxed groups cannot silently launch host MCP processes without an explicit, auditable opt-in. | pass | `internal/mcp.PlanLaunch`; `config.MCPServer.Execution`; `config.Config.AgentPolicy` | `TestSandboxMCPUsesRestrictedExecutor` asserts sandbox+docker wraps Docker and sandbox+host uses restricted dir | explicit host opt-in is config-visible; deterministic launch authority inspected | `go test ./cmd/waffle ./internal/mcp ./internal/config ./internal/sandbox ./internal/selfdev -count=1`; focused tests above; full `go test ./... -count=1` PASS |
| #77.4 | Sandboxed MCP execution has the same or a clearly documented stricter filesystem, network, resource, and lifecycle controls as built-in sandbox tools. | pass | `internal/mcp.WrapDocker` container args (`--network`, bind `/work`, lifecycle); sandbox resource defaults documented | `TestSandboxMCPUsesRestrictedExecutor` asserts network none and `/work` mount; gated `TestDockerSandboxMCPExecution` performs MCP handshake | `WAFFLE_TEST_DOCKER=1 go test ./internal/mcp -run TestDockerSandboxMCPExecution -count=1 -v`; Docker unavailable locally, so no live pass claimed | `go test ./cmd/waffle ./internal/mcp ./internal/config ./internal/sandbox ./internal/selfdev -count=1`; focused tests above; full `go test ./... -count=1` PASS |
| #77.5 | Host MCP processes receive an allowlisted environment rather than the daemon’s complete inherited environment. | pass | `internal/mcp.BuildProcessEnv`; `ConnectRestricted`; `WrapDocker` allowlisted `-e` construction | `TestBuildProcessEnvNoAmbientSecrets`, `TestConnectRestrictedNoAmbientSecrets`, and `TestWrapDockerOnlyAllowlistedEnv` assert ambient secret exclusion | n/a | `go test ./cmd/waffle ./internal/mcp ./internal/config ./internal/sandbox ./internal/selfdev -count=1`; focused tests above; full `go test ./... -count=1` PASS |
| #77.6 | `waffle doctor` or equivalent diagnostics show each MCP server’s effective group scope and execution authority. | pass | `internal/selfdev.Doctor` MCP authority check and rendered server details | `TestDoctorReportsMCPExecutionAuthorities` asserts effective host/sandbox authority and group scope in doctor output | doctor output assertion is deterministic; no daemon required | `go test ./cmd/waffle ./internal/mcp ./internal/config ./internal/sandbox ./internal/selfdev -count=1`; focused tests above; full `go test ./... -count=1` PASS |
| #77.7 | Tests cover a Docker cron group, a completely denied MCP server (not launched), environment isolation, and an explicitly trusted host MCP server. | pass | `config.Config.AgentPolicy(cron)`; `mcpServerPermitted`; `mcp.PlanLaunch`; `ConnectRestricted` | `TestDockerCronGroupRestrictedMCP` constructs Docker cron policy, denies an ineligible server, checks `/work`/network/env, and gates real handshake; `TestDeniedMCPServerNotLaunched`; env isolation tests; trusted host case in launch tests | `WAFFLE_TEST_DOCKER=1 go test ./cmd/waffle -run TestDockerCronGroupRestrictedMCP -count=1 -v`; deterministic portion passes/normal run skips; live Docker unavailable; procedure in `docs/sandbox-queue.md` | `go test ./cmd/waffle ./internal/mcp ./internal/config ./internal/sandbox ./internal/selfdev -count=1`; focused tests above; full `go test ./... -count=1` PASS |
| #78.1 | `spawn_subagent` accepts a `WorkPacket`; a legacy `{task}` call remains valid and behaviour-compatible. | pass | `agent.WorkPacket`; `ParseWorkPacket`; `SubagentTool.Def/Run` | `TestSubagentTool`; `TestFramePacketLegacyCompatible`; `TestParseWorkPacketRejectsUnknownAndOversizedFields` | n/a | `go test ./internal/agent ./internal/session ./internal/workset -count=1`; full `go test ./... -count=1` PASS |
| #78.2 | The child receives the packet in a clearly framed section and never receives the parent transcript implicitly. | pass | `FramePacket`; `SubagentTool.Run` clean `llm.UserText(p.Task)` history | `TestFramePacketLegacyCompatible`; `TestSubagentTool` (single clean task message, no parent transcript) | n/a | `go test ./internal/agent ./internal/session ./internal/workset -count=1`; full `go test ./... -count=1` PASS |
| #78.3 | Final handoffs are validated against a documented schema; malformed output gets at most one repair attempt, then returns an explicit failed handoff. | pass | `ParseHandoff`; `decodeStrictJSON`; `SubagentTool.repairHandoff` | `TestParseHandoffStrictSchema`; `TestParseHandoffRequiresFencedJSON`; `TestHandoffRepairAttempt`; `TestMalformedHandoffRepairFailureReturnsFailed` | n/a | `go test ./internal/agent ./internal/session ./internal/workset -count=1`; full `go test ./... -count=1` PASS |
| #78.4 | `done` is downgraded to `partial` when requested verification is missing or failed, with a machine-readable reason. | pass | `NormalizeHandoff` exact requested-command coverage | `TestNormalizeHandoffDowngradesMissingRequestedCommand`; `TestParseAndNormalizeHandoff`; `TestObservedVerificationDowngrades` | n/a | `go test ./internal/agent ./internal/session ./internal/workset -count=1`; full `go test ./... -count=1` PASS |
| #78.5 | Waffle-executed verification is distinguished from child-reported verification (`observed` vs `reported`). | pass | `runObservedVerification`; `VerificationResult.Observed` | `TestObservedVerificationDowngrades` asserts `observed: true` for Waffle-run bash evidence | n/a | `go test ./internal/agent ./internal/session ./internal/workset -count=1`; full `go test ./... -count=1` PASS |
| #78.6 | Paths outside `OwnedPaths` are surfaced as `needs_supervisor_review`; `ReadOnly=true` plus a changed path is a blocker. | pass | `normalizeRelativePath`; `pathUnderOwned`; `NormalizeHandoff` read-only/supervisor reasons | `TestNormalizeHandoffRejectsOwnedPathTraversalBypass`; `TestParseAndNormalizeHandoff` asserts out-of-scope and read-only downgrade | n/a | `go test ./internal/agent ./internal/session ./internal/workset -count=1`; full `go test ./... -count=1` PASS |
| #78.7 | Proposed working-set entries are carried as proposals only and cannot mutate parent state directly (composes with #68). | pass | `NormalizeHandoff` proposal validation; `FormatHandoffResult` not-applied marker | `TestParentWorksetUnchangedAfterSubagentProposals`; `TestSubagentProposalsDoNotMutateParentSet` | n/a | `go test ./internal/agent ./internal/session ./internal/workset -count=1`; full `go test ./... -count=1` PASS |
| #78.8 | Packet + normalized handoff are persisted with the child session and survive a gateway restart. | pass | `session.PersistSubagentHandoff`; `LoadSubagentHandoffs`; migration `0014_subagent_handoffs_notes_fts.sql` | `TestSubagentPacketHandoffPersistsAcrossStoreReopen` closes/reopens SQLite, decodes both `packet_json` and `handoff_json`, and deep-compares typed values | n/a | `go test ./internal/session -run TestSubagentPacketHandoffPersistsAcrossStoreReopen -count=1`; full `go test ./... -count=1` PASS |
| #78.9 | Parallel subagents can be given disjoint `OwnedPaths`; the parent gets deterministic per-child results regardless of completion order. | pass | `SubagentTool.Run`; `NormalizeHandoff`; per-invocation `WorkPacket.OwnedPaths` | `TestParallelSubagentsDisjointOwnedPathsRemainAssociatedOutOfOrder` releases B before A, asserts A remains done/in-scope and B remains partial with its own supervisor-review violation | n/a | `go test -race ./internal/agent -run TestParallelSubagentsDisjointOwnedPathsRemainAssociatedOutOfOrder -count=1`; full `go test ./... -count=1` PASS |
| #78.10 | Unit tests cover: legacy task-only packet, valid done handoff, malformed→repair, malformed→failed, missing verification downgrade, failed verification downgrade, out-of-scope file, read-only violation and proposal-not-applied. | pass | packet/handoff schema and normalization surfaces above | `TestSubagentTool`; `TestParseAndNormalizeHandoff`; strict-schema, repair success/failure, verification, traversal, read-only, and proposal tests named in #78.1-.9 | n/a | `go test ./internal/agent ./internal/session ./internal/workset -count=1`; full `go test ./... -count=1` PASS |
| #78.11 | `docs/plan.md` documents the typed packet/handoff boundary and explicitly rejects a larger in-tree workflow engine. | pass | `docs/plan.md` Typed subagent packets and handoffs section | manual inspection of `docs/plan.md` typed boundary and explicit workflow-engine rejection | documentation inspected against current code | `go test ./internal/agent ./internal/session ./internal/workset -count=1`; full `go test ./... -count=1` PASS |
| #79.1 | The six tool names and JSON result contracts are documented, including source-authority and staleness rules. | pass | `codeintel.ToolNames`; `CodeLocation`; documented contract in `docs/plan.md` | `TestToolNamesMatchIssue79`; contract/documentation inspection | documentation inspected against current code | `go test ./internal/codeintel ./internal/mcp ./internal/eval ./internal/repopolicy ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
| #79.2 | Waffle can discover the optional tool set and describe it consistently to the agent; partial implementations are allowed and capabilities are explicit. | pass | `codeintel.Toolbox`; `CapabilitiesJSON`; `ToolboxWithCaps` | `TestToolboxDiscovery`; `TestToolboxWithCapsFilters` | n/a | `go test ./internal/codeintel ./internal/mcp ./internal/eval ./internal/repopolicy ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
| #79.3 | A Go workspace reference implementation supports symbol definitions, references and file/package structure with fixture-based tests. | pass | `codeintel.Service.FindSymbol`; `References`; `Structure` | `TestFindSymbolStructureReferences` fixture definitions/references/structure | n/a | `go test ./internal/codeintel ./internal/mcp ./internal/eval ./internal/repopolicy ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
| #79.4 | Blast-radius results are conservative and evidence-backed; uncertainty is represented explicitly rather than fabricated as certainty. | pass | `codeintel.Service.BlastRadius`; `BlastResult.Uncertain/Evidence` | `TestBlastRadiusAndSuggestTestsUncertain` | n/a | `go test ./internal/codeintel ./internal/mcp ./internal/eval ./internal/repopolicy ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
| #79.5 | Every result includes repo/ref/path/line provenance and a `source` classification; cached results include freshness metadata. | pass | `codeintel.location`; `CodeLocation` provenance/source/indexed_at fields | `TestFindSymbolStructureReferences`; `TestCacheStaleAfterEdit` | n/a | `go test ./internal/codeintel ./internal/mcp ./internal/eval ./internal/repopolicy ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
| #79.6 | A test modifies a file after an indexed/cached result and proves stale data is either refreshed or clearly marked and never treated as authoritative over a direct read. | pass | `codeintel.Service.IndexFile`; content-hash cache check in `FindSymbol` | `TestCacheStaleAfterEdit` modifies indexed file and asserts `text-fallback` live authority | n/a | `go test ./internal/codeintel ./internal/mcp ./internal/eval ./internal/repopolicy ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
| #79.7 | The MCP/LSP process runs inside the workspace sandbox or another #77-compliant restricted executor, with a test asserting it does not inherit the gateway’s ambient environment/secrets. | pass | `mcp.PlanLaunch`; `ConnectRestricted`; `BuildProcessEnv`; `WrapDocker` | `TestBuildProcessEnvNoAmbientSecrets`; `TestConnectRestrictedNoAmbientSecrets`; `TestSandboxMCPUsesRestrictedExecutor`; gated Docker MCP tests | Docker runtime gated per docs/sandbox-queue.md; deterministic restricted-env tests pass; no local live pass claimed | `go test ./internal/codeintel ./internal/mcp ./internal/eval ./internal/repopolicy ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
| #79.8 | A repo policy (#53) cannot select an unapproved executable, widen filesystem/network access or launch the server on the host. | pass | `codeintel.ApprovedCapability`; `ValidateMCPServer`; `applyCodeIntelCaps` | `TestValidateMCPServerRejectsSecretsAndHost`; `TestRepoPolicyCannotSelectUnapprovedCodeIntelCaps`; `TestApprovedCapability` | n/a | `go test ./internal/codeintel ./internal/mcp ./internal/eval ./internal/repopolicy ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
| #79.9 | Absence/failure of code intelligence degrades cleanly to the current `search`/`read` workflow; it cannot prevent an otherwise valid workspace from running unless explicitly configured as required by the host. | pass | `evalCodeIntelFallback`; agent toolbox with failing `code_find_symbol`, native `search`/`read_file` | `TestCodeIntelFailureFallsBackToNativeSearchRead`; eval `codeintel_symbol_over_broad_read` now requires failed codeintel, successful native search/read, and completed run | n/a | `go test ./internal/codeintel ./internal/mcp ./internal/eval ./internal/repopolicy ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
| #79.10 | One deterministic agent eval (#63) demonstrates reduced broad-file loading for a caller-sensitive Go change while still verifying against current source. | pass | `evalCodeIntelSymbol`; current-source read/search assertions; `TestCacheStaleAfterEdit` freshness proof | eval `codeintel_symbol_over_broad_read`; `TestCacheStaleAfterEdit` ensures current authoritative source | n/a | `go test ./internal/codeintel ./internal/mcp ./internal/eval ./internal/repopolicy ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
| #79.11 | `docs/plan.md` records the architectural decision: structural code intelligence belongs behind MCP/workspace tooling, not in Waffle’s core storage/runtime. | pass | `docs/plan.md` code-intelligence decision; eval `codeintel_symbol_over_broad_read` | manual `docs/plan.md` decision inspection; eval `codeintel_symbol_over_broad_read` asserts no `noise.go` broad-load result | documentation inspected against current code | `go test ./internal/codeintel ./internal/mcp ./internal/eval ./internal/repopolicy ./cmd/waffle -count=1`; `go run ./cmd/waffle eval`; full `go test ./... -count=1` PASS |
