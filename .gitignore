# build output
/waffle
/bin/
# Stray `go build -o fixtures` output; an 18MB Mach-O binary was committed
# by accident in e7e322b and removed in this change.
/fixtures
/dist/
/.worktrees/
/.superpowers/
/.grok/
/tools/brand-assets/node_modules/

# local state that must never be committed
*.age
*.db
*.db-wal
*.db-shm
.env
.env.*
tools/dashboard-tests/node_modules/
